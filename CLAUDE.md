# service_supplychain 工程约定

供应链后端服务。链路：**销售计划 → 采购单 → 排货 → 投送 → 回执**，外部对接领星 ERP / 亚马逊。

---

## ★★ 实施纪律 —— 杜绝编造非事实要求

```
① 每个数字，要么指到 17/18 的某个 n，要么标「设计取值 · 未实测」
② 每条约束，要么指到 01 的铁律 / 02 的关系 / 00 的裁定，要么不写
③ ★ 指不到出处的 —— 记进 00-待裁定清单，不许自己定，也不许先写着
```

★ **渐进式加载按 `docs/00e` §5 的文档地图** —— 先查「这个问题谁说了算」，再只读那一份。
★ 第 ③ 条写死是因为：「先按合理的写着」的东西不会被改，会被下一个人当成已裁定的事实。
本项目已出过三次 —— 「一个目的地一张」「前端不在本仓」「默认生产 30 天」。

---

## ★ 写代码与写界面的铁律

**不要写详细冗余的功能描述或解说。**

| | 该在哪 |
|---|---|
| 「这个东西是什么、为什么这么设计」 | ★ **docs/**，不在代码注释里，更不在屏幕上 |
| 代码注释 | 只写**为什么这么做**（尤其反直觉处），不复述代码在干什么 |
| 界面文案 | 只写**当场发生的事**：状态、结果、错误、下一步 |

界面上的说明一律进折叠区，默认收起。★ 判据：**屏幕给数据，文档给论证。**

---

## 架构铁律

| # | 约定 | 违反的后果 |
|---|---|---|
| 1 | **分层由测试强制**：`web / api(ui·pub) / shared / rules / forecast / dim / erp / jobs`，不靠口头 | 层会悄悄糊在一起 |
| ★ 1b | ★ **前端在本仓**（Q-1）。`web/` 只打 `api/ui/`；★ `api/pub/` 是给 jxd_ai_ops 的**承诺**，暴露结论不暴露内表 | 改个界面把下游打挂 / 被自己的表结构锁死 |
| 2 | ★ **承重墙**：一张表 + 一个求和函数 + 一个恒等式，三样缺一不可 | 量对不上且没人知道 |
| 3 | ★ **两轴状态**：`our_stage`（我们写）× `lingxing_stage`（只读回）。**不一致本身是信号** | 用一个轴掩盖真相 |
| 4 | ★ **木桶用在我们的动作上，下限用在外部物理事实上** | 到货一件就当全到 |
| 5 | **状态机做成数据**：白名单表 + 外键，非法迁移 = 外键冲突 | 状态从代码里长出分叉 |
| 6 | ★ **人填的东西必须有自己的表；派生物不许承载人的输入** | 输入丢失且无处可查 |
| 7 | ★ **一个步骤的产出，在后续每一步必填；在它之前的步骤不许要求它** | 货悄悄消失 / 要一个不存在的东西 |
| 8 | ★ **只追加的表禁 UPDATE/DELETE**（证据、审计、观察） | 证据可改就不再是证据 |

---

## 数据与外部

| | |
|---|---|
| 写 | PostgreSQL `192.168.66.210` · `jxd_ml` · ★ **独占 schema `scm`** |
| 读 | ClickHouse `192.168.66.211` · `jxd_raw` · ★ **绝不写任何表** |
| 变更 | ★ **只走迁移文件**，已执行的不可改；**不许手工 psql 改结构** |
| 外部 | 领星 OpenAPI（写）· 亚马逊 STA（经领星）· `jxd_edp` **已废弃** |

★ **所有 `📤📥` 是调用，不是我们的功能。** 写成「我们的功能」就会出现「以为发出去了，其实没有」。

## 出站调用

| # | 约定 |
|---|---|
| 1 | ★ **路径前缀定不可逆性**：`/amzStaServer/` 打亚马逊、不可逆；`/erp/` 领星本地 |
| 2 | ★ **超时一律转人工，绝不自动重试** —— `confirmPlacementOption` 无客户侧幂等键，重试就多一个真货件 |
| 3 | ★ **断点续跑**：已拿到单号的步骤跳过 |
| 4 | 海外仓有 `inbound_order_no` 幂等；FBA **没有** → 自建 `idem_key` + 调用前查重 |
| 5 | 日志三问：**打的谁 · 多久 · 怎么失败的**（带 `e.cause?.code`）；★ 静默兜底是最坏的一种 |

## 命名隔离

★ 领星占用的词**禁用**：`inbound` · `shipment` · `shipment_plan` · `allocation`。
`order` 只给采购单，`plan` 只给销售计划。
我们的：**排货表 sheet → 排货单 consignment → 排货行 consignment_line**。

---

## 渐进式加载 —— 按任务读，不要全读

| 在做什么 | 读 |
|---|---|
| ★★ **实施任何东西之前** —— 阶段划分 · 校验判据 · ★ **文档地图（谁对什么说了算）** | `docs/00e-分阶段实施计划.md` |
| 任何改动前，先看有没有未决项 | `docs/00-待裁定清单.md` |
| ★ **在改的这一刀**：货件分层（步骤 · 阻塞 · 作废说法登记） | `docs/00c-货件分层改造计划.md` |
| ★ **前端并入本仓**：栈 · 两类接口 · 认证风险 | `docs/00d-前端并入本仓改造计划.md` |
| 原型踩出来、要落回文档的 | `docs/00b-原型验证带回的变更.md` |
| 分层 / 投送编排 / 不可逆边界 | `docs/01-架构设计.md` |
| ★ 对象、关系（基数 + 聚合语义）、状态事实、动作 | `docs/02-本体实体关系.md` |
| 建表、约束、恒等式、核对视图 | `docs/03-数据库表清单.md` |
| 状态迁移、确认闸、错误码 | `docs/04-状态流转.md` |
| 本体 → 代码的映射 | `docs/05-模块组件.md` |
| 业务流程与两条投送链 | `docs/06-核心业务流程.md` |
| 时序、STA 四步、轮询 | `docs/07-核心业务模块时序.md` |
| 接口签名与错误码 | `docs/08-后端接口清单.md` |
| 用例边界 | `docs/09-项目核心功能用例图.md` |
| 交互原则 · 墨迹语言 · 原型页面清单 | `docs/10-前端交互界面.md` |
| ★ **库存预测**：主公式、四段管道、提前期 | `docs/14-库存预测数学模型.md` |
| ★ **排货**：领域、实体、闭环流程、分单规则 | `docs/15-排货单完整逻辑.md` |
| ★ **谁做什么**：系统内 / 外部 / 原型怎么模拟 | `docs/16-系统边界与外部模拟.md` |

---

## 几条会反复咬人的判据

| 判据 | 来源 |
|---|---|
| ★ **空 ≠ 0**；**「不适用」≠ 0**；**「没有」≠「没算进来」** | 三类都长得一样 |
| ★ **一个数要能回答「它是关于什么的」** —— 结论必须回显依据 | 没人能证伪的数会被当成事实 |
| ★ **不执行的东西不会失败** —— 跳过的测试、没人兑现的担保、覆盖不到的代码 | 它们不会报警 |
| ★ **检查任何过滤 / join / 聚合，必须同时统计被丢掉的那一侧** | 静默丢失已踩六次 |
| ★ **状态机能画出的退回边，必须真的退得回去** | 否则是死胡同 |
| ★ **测试写完先弄失败一次** | 「过了」和「没测到」长得一样 |

---

## 前端与原型

★ **产品前端在 `web/`**（React + Vite + TS）—— 见 `docs/00d`。
`docs/prototype/` 是 ★ **已冻结的老原型**，纯原生 HTML/CSS/JS，只作参考，**不再修**。

```
node test_forecast.js  test_buyer.js  test_dispatch.js  test_css.js  test_doc_flow.js  test_retired_claims.js  test_mermaid.js  test_doc_index.js
NODE_PATH=<jsdom> node test_smoke.js  test_page.js  test_prose.js …
./replay.sh --self-test        # 变异回放器，三道闸
./replay.sh --table <表>        # 整表重跑
```

★ 原型的用途**不是它自己，是验证需求文档** —— 踩出来的每一条都要落回 `docs/`。
★ 前端 `mock | api` 可切换：先跑通交互，接口到位逐个切 —— 不用等后端全做完。
★ **页面操作流程以 `docs/10` §2.5 为准**（按钮原文 + 跳转），`test_doc_flow.js` 两边对核；
  改路径要同时改两边 —— 文档与原型各自「好好的」而互相不符，不会有任何东西失败。
★ 原型里「人工输入」有两种：**用户的活** vs **模拟外部返回**，二者必须一眼分得开。
