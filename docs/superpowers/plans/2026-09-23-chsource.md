# ChSource 接真 ClickHouse Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `dim/ch_source.py::ChSource` 的四个 `NotImplementedError` 换成真 CH 取数，让 `api/ui/plans.py:32` 由配置在 `FixtureSource` 与 `ChSource` 之间切换，而 `dim/source.py` 的协议一个字不改。

**Architecture:** `dim/` 只装 SQL 文本 + 纯变换 + 丢弃计数，`Query = Callable[[str], list[tuple]]` 由 `api/ui/source_factory.py` 注入（这是本仓既有模式，见 `dim/ch_source.py:33-35` 与 `tests/test_layering.py:53-55`）。ChSource 内部按**解析出来的快照日**批量取数并缓存，对外仍是协议那四个单点方法。取不到数一律 503，绝不降级成 200，也绝不用 `0` 冒充「未知」。

**Tech Stack:** Python 3.12 · clickhouse-connect（只经 `shared/ch_client.py`）· FastAPI · pytest · ruff · uv

**Spec:** `docs/superpowers/specs/2026-09-23-chsource-design.md`

## Global Constraints

- **绝不向 ClickHouse 写任何东西。** `dim/` 里出现 `insert into` / `create table` / `alter table` / `drop table`（哪怕在注释字符串里）会让 `tests/test_layering.py::test_l7_no_writes_to_clickhouse` 红。
- **`dim/` 不许 import `shared.ch_client` / `shared.pg_client` / `psycopg2` / `api`**（`tests/test_layering.py:55` 的 `NO_CONNECTIONS`）。客户端由调用方注入。
- **不改 `dim/source.py`**：四个签名与返回类型固定为 `as_of() -> dt.date`、`monthly_sales_history(seller_sku: str, sid: str, months: int) -> list[tuple[dt.date, int]]`、`onhand_available(seller_sku: str, sid: str) -> int | None`、`purchase_in_transit(sku: str) -> list[InTransit]`。
- **不改 `dim/fixture_source.py`，不改 `tests/fixtures/` 里的任何一个字节。**
- **每一处过滤 / join 都必须统计被丢弃的那一侧**，并把计数打进日志；认不出的形态硬失败，不许落进 `else ''`。
- **每次外部调用留三问日志**：打的谁（host/db）· 多久（elapsed_ms）· 怎么失败的（`describe_failure` 的 `kind` + errno）。
- **未知 ≠ 0 ≠ 不适用。** `onhand_available` 只在「这批快照被接受了、里面确实没有这个 msku」时返回 `0`；拿不到数一律抛异常。采购单快照陈旧（Task 3 的 `PurchaseTableStale`，OQ-5 裁定）同属这一条——超阈值一律未知，不许拿旧数字或 `0` 顶上。
- **不发明分配规则。** `sid=0` 欧洲共享池（27.6% 可售）不得按单店给全额或平摊（OQ-2 裁定，C4 禁），逾期在途（OQ-6 裁定）不得重定到货日折进未来月份——发明规则与猜一个数是同一类错误，都不许做。
- **失败翻译只用既有的 `ApiError` 一条路径**：新失败类型在调用方（`api/ui/plans.py`）catch 住再 `raise ApiError(...)`，照抄 `api/ui/ops.py::refresh_dims_now` 的形状；**不**在 `api/__init__.py` 新增 `@app.exception_handler`——那会让本仓的错误响应从一条路径分叉成两条。
- **测试写完先弄失败一次**，每个 RED 步骤都要看到指定的失败信息，看不到就说明测试没测到目标。
- 验证命令：`uv run pytest -q -rs` · `uv run ruff check .` · `uv run pytest -q tests/test_layering.py`
- 所有命令在 `/home/fido/work/2026/jxd_service_group/service_supplychain` 下跑。**不要自己起或重启服务**；需要起服务时把命令交给使用者。

---

### Task 1: as_of —— 快照日解析 + 掉档守卫

**Files:**
- Modify: `dim/ch_source.py`（在文件末尾新增「预测取数」段，`ChSource` 类整体重写）
- Modify: `config.example.toml`（新增 `[forecast]` 段）
- Modify: `shared/config.py`（新增 `forecast()` 读取函数 + 默认值）
- Test: `tests/test_dim_ch_source_forecast.py`（新建）
- Test: `tests/test_config_example.py`（既有：`[forecast]` 六键双向相等）

**Interfaces:**
- Consumes: `dim.ch_source.Query`（已存在，`Callable[[str], list[tuple]]`）、`dim.ch_source.UnknownShape`（已存在）
- Produces:
  - `class ChUnavailable(Exception)` —— CH 连不上/超时，构造 `ChUnavailable(target: str, cause: dict)`
  - `class ChDataUnusable(Exception)` —— 候选日全被拒，构造 `ChDataUnusable(rejected: list[dict])`，每个 dict 形如 `{"date": "2026-09-23", "rows": 4100, "uniq_sid": 12, "reason": "coverage_drop"}`
  - `SQL_FBA_CAPTURE_DAYS: str` —— 候选日查询文本
  - `pick_snapshot_date(rows, now, threshold) -> tuple[dt.date, list[dict]]` —— 纯函数，返回 (被接受的日期, 被拒日清单)
  - `class ChSource` 带 `__init__(self, query: Query, *, now: Callable[[], dt.datetime] = _utc_now, drop_threshold: float = 0.30, cache_ttl_s: int = 300)` 与 `as_of(self) -> dt.date`
  - `shared.config.forecast() -> dict`，键：`source`（`"fixture"`｜`"ch"`）、`snapshot_lookback_days`（7）、`snapshot_settle_minutes`（30）、`cache_ttl_seconds`（300）、`sales_months_max`（24）、`drop_threshold`（0.30）

★ **日报型表护栏（邻仓 CLAUDE.md「取数的四条铁律 4」）**：`lingxing_inventory_fba_detail` 每日全量、
会掉档。`pick_snapshot_date` 本身就是这条护栏的落点——② 比对相邻两日的行数与 `uniq(sid)`
（`coverage_drop_rows` / `coverage_drop_uniq_sid`）覆盖「比对相邻两日覆盖数」，① 按
`_captured_date` 取整日切片、不跨日聚合覆盖「先去重」。这一条不是新增要求，是钉死：**不许**
为了图省事把 `pick_snapshot_date` 简化成只比总行数——整店 0 行的掉档总行数可能看不出来
（Task 1 Step 1 的 `test_rejects_a_day_that_lost_stores_even_when_row_count_holds` 已经守着这条）。

- [ ] **Step 1: 写失败的测试**

`tests/test_dim_ch_source_forecast.py`：

```python
"""ChSource 预测取数的纯变换。★ 假 query 只按 SQL 的关键字回放，不真连 CH。"""
from __future__ import annotations

import datetime as dt

import pytest

from dim import ch_source as cs

#: 实测形状（design §1.3 E-1/E-3）：近 20 个采集日 7,934~8,080 行、每日恰好 21 个 sid，
#: 当天 06:34~07:39 起跑、约 60 秒跑完。
FULL = 8000
NOW = dt.datetime(2026, 9, 23, 12, 0, 0)


def day(d: int, rows: int = FULL, sids: int = 21, done_at: str = "06:35:00"):
    return (dt.date(2026, 9, d), rows, sids,
            dt.datetime.fromisoformat(f"2026-09-{d:02d}T{done_at}"))


def test_picks_the_newest_settled_day():
    got, rejected = cs.pick_snapshot_date([day(23), day(22), day(21)], NOW, 0.30)
    assert got == dt.date(2026, 9, 23)
    assert rejected == []


def test_rejects_a_half_written_batch_and_names_it():
    """★ 半写的当天批次：行数只有一半 —— 必须退一天，且退这一步要留下证据。"""
    got, rejected = cs.pick_snapshot_date([day(23, rows=4000), day(22), day(21)], NOW, 0.30)
    assert got == dt.date(2026, 9, 22)
    assert rejected == [{"date": "2026-09-23", "rows": 4000, "uniq_sid": 21,
                         "reason": "coverage_drop_rows"}]


def test_rejects_a_batch_still_being_written():
    """★ 行数够了但刚写完 30 分钟不到 —— 同样不能用（design §4.1 判据 1）。"""
    now = dt.datetime(2026, 9, 23, 6, 50, 0)
    got, rejected = cs.pick_snapshot_date([day(23), day(22), day(21)], now, 0.30)
    assert got == dt.date(2026, 9, 22)
    assert rejected[0]["reason"] == "not_settled"


def test_rejects_a_day_that_lost_stores_even_when_row_count_holds():
    """★ 整店 0 行是采集缺口的形状，总行数看不出来（jobs/refresh_dims.py:150-153 同一条）。"""
    got, rejected = cs.pick_snapshot_date([day(23, sids=12), day(22), day(21)], NOW, 0.30)
    assert got == dt.date(2026, 9, 22)
    assert rejected[0]["reason"] == "coverage_drop_uniq_sid"


def test_all_candidates_rejected_raises_with_every_number():
    days = [day(23, rows=100), day(22, rows=100), day(21, rows=100)]
    with pytest.raises(cs.ChDataUnusable) as e:
        cs.pick_snapshot_date(days, NOW, 0.30)
    assert len(e.value.rejected) == 3
    assert "2026-09-23" in str(e.value)


def test_empty_candidate_list_is_not_a_silent_empty():
    with pytest.raises(cs.UnknownShape, match="lingxing_inventory_fba_detail"):
        cs.pick_snapshot_date([], NOW, 0.30)


def test_as_of_caches_within_ttl_and_rebuilds_after():
    calls = []

    def q(sql: str):
        calls.append(sql)
        return [day(23), day(22)]

    clock = [dt.datetime(2026, 9, 23, 12, 0, 0)]
    src = cs.ChSource(q, now=lambda: clock[0], cache_ttl_s=300)
    assert src.as_of() == dt.date(2026, 9, 23)
    assert src.as_of() == dt.date(2026, 9, 23)
    assert len(calls) == 1, "TTL 内重复解析 = 每次 grid 都多打一次 CH"
    clock[0] += dt.timedelta(seconds=301)
    src.as_of()
    assert len(calls) == 2, "TTL 过了还不重建 = 缓存在保留陈旧"
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py -rs`
Expected: FAIL，`AttributeError: module 'dim.ch_source' has no attribute 'pick_snapshot_date'`（7 条全红）。

- [ ] **Step 3: 写实现**

`dim/ch_source.py` 末尾追加：

```python
# ---------------------------------------------------------------------------
# 预测取数（design 2026-09-23）。★ 与上面的镜像刷新是两件事：镜像是慢变维度，
#   这里是带时点的量 —— 每个数都要能回答「它是关于什么的」。
# ---------------------------------------------------------------------------

#: 候选快照日。★ 只问 fba_detail —— as_of 标注的是**在仓数**，它的时点就是这张表的。
SQL_FBA_CAPTURE_DAYS = """
SELECT _captured_date, count() AS rows, uniq(sid) AS uniq_sid, max(_captured_at) AS done_at
  FROM jxd_raw.lingxing_inventory_fba_detail
 WHERE _captured_date >= today() - {lookback}
 GROUP BY _captured_date
 ORDER BY _captured_date DESC
"""

#: ★ 实测：该表当天 06:34~07:39 起跑、约 60 秒跑完（design §1.3 E-3），而刷新调度
#:   就排在 06:30 —— 半写窗口是真实存在的。30 分钟 = 实测时长的 30 倍。
SETTLE_MINUTES = 30
LOOKBACK_DAYS = 7


class ChUnavailable(Exception):
    """CH 连不上/超时。★ 带上 describe_failure 的分类：「超时」与「连不上」处置相反。"""

    def __init__(self, target: str, cause: dict) -> None:
        self.target, self.cause = target, cause
        super().__init__(f"ch_unavailable target={target} cause={cause}")


class ChDataUnusable(Exception):
    """候选日全被拒。★ 带上每一天的数字 —— 「不可用」不说明是哪一种不可用就没法查。"""

    def __init__(self, rejected: list[dict]) -> None:
        self.rejected = rejected
        super().__init__(f"ch_data_unusable rejected={rejected}")


def _utc_now() -> dt.datetime:
    return dt.datetime.now()


def pick_snapshot_date(rows: list[tuple], now: dt.datetime,
                       threshold: float) -> tuple[dt.date, list[dict]]:
    """从新到旧挑第一个「已采完 + 没掉档」的采集日。返回 (日期, 被拒清单)。

    ★ 被拒清单不是可选的返回值 —— 回退可以，必须有声（CLAUDE.md 判据五）。
    ★ 掉档比的是**下一个更旧的候选日**，不是历史最大值：只往上爬的阈值会把
      「一直在掉」读成「一直没掉」。
    """
    if not rows:
        raise UnknownShape(
            "lingxing_inventory_fba_detail 近 %d 天一个采集日都没有。"
            "★ 空不是「今天没货」——把采集缺口读成空会让整张表的在仓变成 0" % LOOKBACK_DAYS)
    ordered = sorted(rows, key=lambda r: r[0], reverse=True)
    limit = 1.0 - threshold
    rejected: list[dict] = []
    for i, (date, n_rows, n_sid, done_at) in enumerate(ordered):
        reason = None
        if done_at is None or now - done_at < dt.timedelta(minutes=SETTLE_MINUTES):
            reason = "not_settled"
        else:
            nxt = ordered[i + 1] if i + 1 < len(ordered) else None
            if nxt is not None and n_rows < nxt[1] * limit:
                reason = "coverage_drop_rows"
            elif nxt is not None and n_sid < nxt[2] * limit:
                reason = "coverage_drop_uniq_sid"
        if reason is None:
            return date, rejected
        rejected.append({"date": date.isoformat(), "rows": int(n_rows),
                         "uniq_sid": int(n_sid), "reason": reason})
    raise ChDataUnusable(rejected)


class ChSource:
    """真 CH 取数。★ 客户端由调用方注入 —— dim/ 不许 import shared.ch_client。"""

    def __init__(self, query: Query, *, now: Callable[[], dt.datetime] = _utc_now,
                 drop_threshold: float = 0.30, cache_ttl_s: int = 300) -> None:
        self._q = query
        self._now = now
        self._threshold = drop_threshold
        self._ttl = dt.timedelta(seconds=cache_ttl_s)
        self._as_of: tuple[dt.datetime, dt.date] | None = None

    def as_of(self) -> dt.date:
        now = self._now()
        if self._as_of is not None and now - self._as_of[0] < self._ttl:
            return self._as_of[1]
        rows = self._q(SQL_FBA_CAPTURE_DAYS.format(lookback=LOOKBACK_DAYS))
        date, rejected = pick_snapshot_date(rows, now, self._threshold)
        for r in rejected:
            log.warning("op=ch_as_of outcome=rejected %s accepted=%s —— "
                        "回退了一天，这条就是它的证据", r, date)
        log.info("op=ch_as_of outcome=ok as_of=%s candidates=%d rejected=%d",
                 date, len(rows), len(rejected))
        self._as_of = (now, date)
        return date
```

同时在文件顶部补 `import logging` 与 `log = logging.getLogger("scm.dim")`（若已有则复用），并**删掉**旧的 `ChSource` 占位类与 `_MSG` 常量。

`shared/config.py` 追加：

```python
#: ★ 设计取值。`source` 默认 "fixture" —— 默认值必须是离线跑得通的那个，
#:   否则任何一次漏配都会变成「测试去连生产 CH」。
_FORECAST_DEFAULTS = {
    "source": "fixture",
    "snapshot_lookback_days": 7,
    "snapshot_settle_minutes": 30,
    "cache_ttl_seconds": 300,
    "sales_months_max": 24,
    "drop_threshold": 0.30,
}


def forecast() -> dict:
    return {**_FORECAST_DEFAULTS, **_cfg().get("forecast", {})}
```

`config.example.toml` 追加：

```toml
[forecast]
# ★ 阶段 A 取数源。"fixture" = tests/fixtures 的 5 个假 msku（离线可跑）；
#   "ch" = 真 ClickHouse。默认 fixture —— 漏配不该变成「去连生产」。
source = "fixture"

# 快照日候选窗口，以及「这批采集算写完了」的静置时长。
# ★ 实测：lingxing_inventory_fba_detail 当天 06:34~07:39 起跑、约 60 秒跑完，
#   而 [freshness] refresh_at = "06:30" 排在它之前 —— 半写窗口真实存在。
snapshot_lookback_days  = 7
snapshot_settle_minutes = 30

# ChSource 内部缓存：快照批次按采集日缓存（同一天的快照不可变），
# as_of 的解析结果按这个秒数缓存。
cache_ttl_seconds = 300

# 一次最多回溯多少个完整自然月。
sales_months_max = 24

# 掉档阈值。★ 实测该表日间波动 < 2%、每日恰好 21 个 sid，所以 0.30 已经很松。
drop_threshold = 0.30
```

- [ ] **Step 4: 跑测试，确认绿**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py tests/test_config_example.py tests/test_layering.py -rs`
Expected: PASS。若 `test_config_example.py` 红，说明它钉的是 `[freshness]` 六键而不认识 `[forecast]` —— 按它现有的写法给 `[forecast]` 补一条同形的双向相等断言。

- [ ] **Step 5: 提交**

```bash
git add dim/ch_source.py shared/config.py config.example.toml tests/test_dim_ch_source_forecast.py tests/test_config_example.py
git commit -m "feat(dim): ChSource.as_of —— 按掉档守卫挑快照日，回退必留痕"
```

---

### Task 2: onhand_available —— 批量在仓 + sid=0 排除 + 0/未知分开

**Files:**
- Modify: `dim/ch_source.py`
- Test: `tests/test_dim_ch_source_forecast.py`

**Interfaces:**
- Consumes: Task 1 的 `ChSource.__init__`、`ChSource.as_of()`、`ChDataUnusable`
- Produces:
  - `SQL_FBA_ONHAND: str` —— 带 `{as_of}` 占位
  - `parse_onhand(rows) -> tuple[dict[tuple[str, str], int], dict[str, int]]` —— 返回 (`(sid, seller_sku) -> units`, 丢弃理由计数)
  - `ChSource.onhand_available(self, seller_sku: str, sid: str) -> int | None`
  - `ChSource.stats(self) -> dict` —— 最近一次批量的丢弃计数，供上层写进日志与响应

### ★ 控制器裁定追加（09-23，见 spec §8 OQ-2 / OQ-4）

- **计数键名固定为 `shared_pool_excluded`**（不是随便起的内部变量名）：`parse_onhand` 的丢弃字典
  用嵌套形式 `dropped["shared_pool_excluded"] = {"rows": ..., "units": ...}`，下面 Step 3 的实现与
  Step 1 的测试断言都按这个键名写。★ 这个键名要一路带到 Task 5 的 `/grid` 响应 `source_notes.dropped`
  里，不许在装配层改名或再包一层——改名就是把「27.6% 被排除」这件事重新藏起来一次。
- **归属未裁定（OQ-2）**：阶段 A 只做「排除 + 计数」，代码里不许出现「按比例分给 PL/SE」
  一类的占位逻辑或 TODO——那是在没有裁定前先把猜测焊进代码，属于「先写着以后再改」的老毛病
  （`00e-分阶段实施计划.md` §7②）。
- **0 与未知的分界维持原判**：批次被接受且这个 msku 缺行 → `0`；批次不可用 → 抛异常，
  绝不返回 `0` 冒充未知。这条已经是控制器要的形状，Step 3 原方案不用改。
- **OQ-4 连带确认**：本任务只读 `afn_fulfillable_quantity`，不碰 `available_total` /
  `total` 任何一列，也不做任何「用总量减分项」的算术——`available_total` 对不上的 12% 差额
  与本任务无关，不要为了「对齐」去猜一个换算系数。
- **日报型表护栏（CLAUDE.md 铁律 4）**：「比对相邻两日行数/SKU 覆盖数」由 Task 1 的
  `pick_snapshot_date` 在快照日这一层把关，本任务只对**单日内部**的重复行负责——`parse_onhand`
  的 `rows_collapsed` 计数就是这条护栏在本任务里的落点，不需要在这里重新比对相邻两日。

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_dim_ch_source_forecast.py`：

```python
#: ★ 实测形状（design §1.3 E-4）：sid=0 是欧洲共享池
#:   （name='PETSFIT-JXD-UK欧洲仓'，seller_group_name='PETSFIT-JXD-PL,PETSFIT-JXD-SE'），
#:   1,114 行 / 14,073 件，占全部可售 50,947 件的 27.6%。它不是任何单店的在仓。
ONHAND_ROWS = [
    ("11072", "DCC1800264G1Z2B", 672, 1),
    ("11072", "DVCD105013ALZ2B", 1323, 1),
    ("0", "002F2RedM", 14073, 1114),      # ← 共享池，必须被排除且计数
    ("11094", "MSKU-UK-1", 0, 1),         # ← 真的是 0，不是缺行
]


def onhand_query(rows=None, days=None):
    def q(sql: str):
        if "lingxing_inventory_fba_detail" in sql and "_captured_date >=" in sql:
            return days if days is not None else [day(23), day(22)]
        if "afn_fulfillable_quantity" in sql:
            return ONHAND_ROWS if rows is None else rows
        raise AssertionError(f"没预料到的 SQL：{sql[:80]}")
    return q


def test_shared_pool_sid_zero_is_excluded_and_counted():
    """★ 27.6% 的可售被排除，不留痕就再也没人想得起来它去哪了。"""
    by_key, dropped = cs.parse_onhand(ONHAND_ROWS)
    assert ("0", "002F2RedM") not in by_key
    assert dropped == {"shared_pool_excluded": {"rows": 1114, "units": 14073}}


def test_a_real_zero_is_zero_not_missing():
    by_key, _ = cs.parse_onhand(ONHAND_ROWS)
    assert by_key[("11094", "MSKU-UK-1")] == 0


def test_msku_absent_from_an_accepted_batch_is_zero():
    """★ L-1：FBA 报表不返回库存为 0 的 SKU —— 缺行 = 0，而断货正是最该被看见的状态。"""
    src = cs.ChSource(onhand_query())
    assert src.onhand_available("NEVER-STOCKED", "11072") == 0


def test_onhand_reads_the_snapshot_once_for_many_mskus():
    calls = []
    base = onhand_query()

    def q(sql):
        calls.append(sql)
        return base(sql)

    src = cs.ChSource(q)
    for msku in ("DCC1800264G1Z2B", "DVCD105013ALZ2B", "NEVER-STOCKED"):
        src.onhand_available(msku, "11072")
    assert sum("afn_fulfillable_quantity" in s for s in calls) == 1, (
        "grid 逐 msku 调 21 次 —— 不批量就是 21 次往返")


def test_unusable_batch_raises_instead_of_returning_zero():
    """★ 这条是整个设计的支点：拿不到数时返回 0 会让「断货」和「查不到」长得一样。"""
    bad = [day(23, rows=100), day(22, rows=100)]
    src = cs.ChSource(onhand_query(days=bad))
    with pytest.raises(cs.ChDataUnusable):
        src.onhand_available("DCC1800264G1Z2B", "11072")


def test_duplicate_rows_are_summed_and_counted():
    """★ 今天一行不重（实测 8,080/8,080 n=1），但重复行是 L-4 点名的形态。"""
    rows = [("11072", "X", 10, 1), ("11072", "X", 5, 1)]
    by_key, dropped = cs.parse_onhand(rows)
    assert by_key[("11072", "X")] == 15
    assert dropped["rows_collapsed"] == 1
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py -k onhand -rs`
Expected: FAIL，`AttributeError: module 'dim.ch_source' has no attribute 'parse_onhand'`。

- [ ] **Step 3: 写实现**

```python
#: ★ 列裁定：`afn_fulfillable_quantity` = 可售在仓。
#:   不用 `available_total`（实测 972/8,080 行更大，含预留与不可售）；
#:   不用 `total`（含 inbound，而 inbound 在 grid 里是另一列）。
#: ★ 不按 `sku` 关联：该列实测 1,932/8,080（23.9%）为空。
#: ★ `sid` 与 `seller_sku` 都出，且 sid 转 String —— PG 侧 seller_id 是 text（001），
#:   以字符串为准的一侧不做数值解析。
SQL_FBA_ONHAND = """
SELECT toString(sid)                              AS sid,
       seller_sku                                 AS seller_sku,
       toInt64(sum(afn_fulfillable_quantity))     AS units,
       count()                                    AS raw_rows
  FROM jxd_raw.lingxing_inventory_fba_detail
 WHERE _captured_date = toDate('{as_of}')
 GROUP BY sid, seller_sku
"""

#: ★ sid=0 是欧洲共享池（PL+SE 合池），不是任何单店的在仓。
#:   折进任何一个 sid 都是凭空给它 14,073 件（实测占全部可售 27.6%）。
#:   归属未裁定 —— design §8 OQ-2。
SHARED_POOL_SID = "0"


def parse_onhand(rows: list[tuple]) -> tuple[dict[tuple[str, str], int], dict[str, int]]:
    out: dict[tuple[str, str], int] = {}
    dropped: dict[str, int] = {}
    for sid, seller_sku, units, raw_rows in rows:
        sid = str(sid)
        if sid == SHARED_POOL_SID:
            bucket = dropped.setdefault("shared_pool_excluded", {"rows": 0, "units": 0})
            bucket["rows"] += int(raw_rows)
            bucket["units"] += int(units)
            continue
        if int(raw_rows) > 1:
            dropped["rows_collapsed"] = dropped.get("rows_collapsed", 0) + int(raw_rows) - 1
        key = (sid, seller_sku)
        out[key] = out.get(key, 0) + int(units)
        # ★ 同一 key 在 GROUP BY 之后仍出现两次 = SQL 或源变了，这是拼接不是覆盖，
        #   而拼接的那一次也要能被看见。
    return out, dropped
```

`ChSource` 内加缓存与方法：

```python
    def _onhand(self) -> dict[tuple[str, str], int]:
        as_of = self.as_of()
        if self._onhand_cache is not None and self._onhand_cache[0] == as_of:
            return self._onhand_cache[1]
        rows = self._q(SQL_FBA_ONHAND.format(as_of=as_of.isoformat()))
        if not rows:
            raise UnknownShape(
                f"lingxing_inventory_fba_detail 在 {as_of} 取回 0 行 —— "
                "这一天通过了掉档守卫却是空的，说明守卫和取数读的不是同一批")
        by_key, dropped = parse_onhand(rows)
        log.info("op=ch_onhand as_of=%s keys=%d dropped=%s", as_of, len(by_key), dropped)
        self._onhand_cache = (as_of, by_key)
        self._stats["onhand"] = dropped
        return by_key

    def onhand_available(self, seller_sku: str, sid: str) -> int | None:
        # ★ 批次被接受 ⇒ 缺行就是 0（L-1：FBA 报表不返回库存为 0 的 SKU）。
        #   批次不可用 ⇒ _onhand() 已经抛了，绝不会走到这里返回 0。
        return self._onhand().get((str(sid), seller_sku), 0)
```

`__init__` 里补 `self._onhand_cache: tuple[dt.date, dict] | None = None` 与 `self._stats: dict = {}`，并加 `def stats(self) -> dict: return dict(self._stats)`。

- [ ] **Step 4: 跑测试，确认绿**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py tests/test_layering.py -rs`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add dim/ch_source.py tests/test_dim_ch_source_forecast.py
git commit -m "feat(dim): onhand_available 批量取数 —— 排除 sid=0 共享池并计数，缺行是 0 不是未知"
```

---

### Task 3: purchase_in_transit —— 按采购单状态过滤，不按数量算术

**Files:**
- Modify: `dim/ch_source.py`
- Test: `tests/test_dim_ch_source_forecast.py`

**Interfaces:**
- Consumes: Task 1 的 `ChSource`、Task 2 的 `_stats`
- Produces:
  - `SQL_PURCHASE_AS_OF: str`、`SQL_PURCHASE_IN_TRANSIT: str`
  - `parse_in_transit(rows) -> tuple[dict[str, list[InTransit]], dict[str, int]]`
  - `ChSource.purchase_in_transit(self, sku: str) -> list[InTransit]`
  - `ChSource.purchase_as_of(self) -> dt.date` —— 供上层写进 `sku_pipeline`
  - `class PurchaseTableStale(Exception)` —— 陈旧阈值被突破，构造
    `PurchaseTableStale(captured: dt.date, age_days: int, threshold_days: int)`
  - `is_overdue(period: str, as_of: dt.date) -> bool` —— 纯函数，`period`（`YYYY-MM`）早于
    `as_of` 所在月即逾期

### ★ 控制器裁定追加（09-23，见 spec §8 OQ-5 / OQ-6，与 spec §4.4 已有条款一起执行）

- **陈旧阈值（OQ-5 的答案，覆盖原草案「本期只打日志不拦」）**：`purchase_as_of()` 解析出
  `max(_captured_date)` 后，与 `ChSource.as_of()`（fba_detail 的快照日）比较年龄；超过
  `[forecast] purchase_staleness_days`（新增配置键，阶段 A 默认 **3** —— E-14 实测两表本来就
  相差 1 天，3 天给出「漏采一天仍可用、漏采两天就该报警」的余量）就 `raise PurchaseTableStale(...)`，
  由 Task 5 翻成 503，**不许把陈旧数据当成「在途为 0」或悄悄返回上一次缓存的数字**。
  `config.example.toml [forecast]` 追加一行：
  ```toml
  # 采购单快照的陈旧阈值：超过这么多天没有新采集日，整批在途视为「未知」而不是 0。
  # ★ 实测两表（purchase_order_list / _items）比 fba_detail 晚 1 天是常态；3 天留出
  #   「漏采一天仍可用、漏采两天就该报警」的余量（design §8 OQ-5）。
  purchase_staleness_days = 3
  ```
  `shared/config.py::_FORECAST_DEFAULTS` 同步加 `"purchase_staleness_days": 3`。
- **逾期在途单独归桶（OQ-6 的答案）**：`SQL_PURCHASE_IN_TRANSIT` 与 `parse_in_transit` **保持
  原样**——`period` 仍然是 `expect_arrive_time` 的真实（哪怕已过去的）`YYYY-MM`，**绝不改写成
  未来月份**（这条原方案本来就没做错，这里只是钉死不许改）。真正要补的是 Task 5：`api/ui/plans.py`
  组装 `sku_pipeline` 时，用 `is_overdue(t.period, purchase_as_of)` 给每个 pipeline 槽位加一个
  `"bucket": "overdue" | "future"` 字段——`InTransit`（`dim/source.py`）本身不许加字段（协议不改，
  Global Constraints 第 3 条），归桶标记只能在装配层（`api/ui/plans.py`）现算。E-13 实测
  28,216/28,561 件（98.8%）会落进 `overdue` 桶，这本身是数据的形状，不是 bug，但**不许因为
  它不匹配任何一个计划月份就从 `sku_pipeline` 里悄悄消失**——当前草案的
  `pipeline[(sku, t.period)]` 已经是按真实 `period` 建槽位、不会丢，这一条只是要求补上
  `bucket` 字段，让前端不用自己拿今天的日期去算「过期了吗」。
- **日报型表的去重/掉档护栏**（邻仓 CLAUDE.md「取数的四条铁律 4」）：`purchase_order_list` /
  `_items` 是覆盖型表，**没有历史快照可比对相邻两日**（OQ-5 原文），所以「① 先按
  `_captured_date` 去重」这一步已经由 `SQL_PURCHASE_AS_OF` 取 `max(_captured_date)` 单日切片
  天然满足；但「② 比对相邻两日的行数与 SKU 覆盖数」在覆盖型表上做不到——**这正是陈旧阈值
  要顶上的那个位置**：比不了「掉档」，就比「多久没更新」。Step 1 的测试要覆盖：陈旧时抛
  `PurchaseTableStale` 而不是静默用旧数字。

- [ ] **Step 1: 写失败的测试**

```python
#: ★ 实测形状（design §1.3 E-12/E-13/E-14）：
#:   · status=9（已完成）的行项 quantity_receive **全为 0** —— 966 行 / 104,479 件。
#:     只按 `quantity_real - quantity_receive > 0` 取在途，会多出真实开口量的 3.7 倍。
#:   · status=-1（已作废）468 行 / 39,375 件，receive == real，恰好净为 0 —— 但不许靠它。
#:   · expect_arrive_time 有 1 行为 NULL（50 件）。
PURCHASE_ROWS = [
    ("DVCD105013L1", "2026-10", 600, "PO260514011"),
    ("DVCD105013L1", "2026-11", 240, "PO260514011"),
    ("DVCD105013AL", "2026-10", 500, "PO260721011"),
    ("NO-ETA-SKU", None, 50, "PO260514011"),      # ← expect_arrive_time 为 NULL
]


def test_missing_eta_is_dropped_loudly_not_silently():
    """★ 认不出的形态不许落进 else '' 再被下游过滤掉 —— 丢了必须数得出来。"""
    by_sku, dropped = cs.parse_in_transit(PURCHASE_ROWS)
    assert "NO-ETA-SKU" not in by_sku
    assert dropped == {"missing_eta_rows": 1, "missing_eta_units": 50}


def test_in_transit_rows_keep_their_order_number():
    by_sku, _ = cs.parse_in_transit(PURCHASE_ROWS)
    assert by_sku["DVCD105013L1"] == [
        cs.InTransit("2026-10", 600, "PO260514011"),
        cs.InTransit("2026-11", 240, "PO260514011"),
    ]


def test_sql_filters_on_order_status_not_on_quantity_arithmetic():
    """★ 这条守的是 E-12 那个坑：已完成单的 quantity_receive 全是 0。
    光靠 `real > receive` 会把 104,479 件已到货的货算成在途。"""
    sql = " ".join(cs.SQL_PURCHASE_IN_TRANSIT.split())
    assert "argMax(status" in sql, "必须按采购单当前状态判，不是按行项算术"
    assert "o.st = 2" in sql, "只有「待到货」算在途"
    assert "is_delete" in sql


def test_unknown_sku_returns_empty_list_not_none():
    def q(sql):
        if "_captured_date >=" in sql:
            return [day(23), day(22)]
        if "max(_captured_date)" in sql and "purchase_order_list_items" in sql:
            return [(dt.date(2026, 9, 22),)]
        if "quantity_receive" in sql:
            return PURCHASE_ROWS
        raise AssertionError(sql[:80])

    src = cs.ChSource(q)
    assert src.purchase_in_transit("NOT-A-SKU") == []
    assert src.purchase_as_of() == dt.date(2026, 9, 22)
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py -k "transit or eta or status" -rs`
Expected: FAIL，`AttributeError: ... 'parse_in_transit'`。

- [ ] **Step 3: 写实现**

```python
#: ★ 采购表是覆盖型、没有历史快照（docs/01:94），且实测比 fba_detail 晚一天
#:   （2026-09-22 vs 09-23）。所以它用自己的 max(_captured_date)：强行对齐会在
#:   采集没跑的那天返回空，而「空」与「真的没有在途」长得一模一样。
SQL_PURCHASE_AS_OF = """
SELECT max(_captured_date) FROM jxd_raw.lingxing_purchase_order_list_items
"""

#: ★★ 必须按采购单 `status` 过滤，不能只靠 `quantity_real > quantity_receive`。
#:    实测（2026-09-23）：status=9（已完成）的 966 行 / 104,479 件，行项
#:    `quantity_receive` **全为 0** —— 只按算术会把这批已到货的货报成在途，
#:    是真实开口量 28,561 件的 3.7 倍。status=-1（已作废）也显式排除并计数，
#:    不依赖「它恰好净为 0」。
SQL_PURCHASE_IN_TRANSIT = """
WITH o AS (
    SELECT order_sn, argMax(status, _captured_date) AS st
      FROM jxd_raw.lingxing_purchase_order_list
     GROUP BY order_sn
)
SELECT i.sku                                                  AS sku,
       if(i.expect_arrive_time IS NULL, NULL,
          formatDateTime(i.expect_arrive_time, '%Y-%m'))      AS period,
       toInt64(sum(i.quantity_real - i.quantity_receive))     AS units,
       i.order_sn                                             AS ref
  FROM jxd_raw.lingxing_purchase_order_list_items AS i
 INNER JOIN o ON o.order_sn = i.order_sn
 WHERE i._captured_date = toDate('{purchase_as_of}')
   AND o.st = 2
   AND ifNull(i.is_delete, 0) = 0
   AND i.quantity_real > i.quantity_receive
 GROUP BY sku, period, ref
 ORDER BY sku, period, ref
"""


def parse_in_transit(rows: list[tuple]) -> tuple[dict[str, list[InTransit]], dict[str, int]]:
    out: dict[str, list[InTransit]] = {}
    dropped: dict[str, int] = {}
    for sku, period, units, ref in rows:
        if not period:
            # ★ 预计到货日为空的那一行（实测 1 行 / 50 件）：丢弃必须点名，
            #   不许悄悄少一批货。
            dropped["missing_eta_rows"] = dropped.get("missing_eta_rows", 0) + 1
            dropped["missing_eta_units"] = dropped.get("missing_eta_units", 0) + int(units)
            continue
        out.setdefault(sku, []).append(InTransit(period, int(units), ref))
    return out, dropped
```

`ChSource` 内：

```python
    def purchase_as_of(self) -> dt.date:
        if self._purchase_as_of is None:
            rows = self._q(SQL_PURCHASE_AS_OF)
            if not rows or rows[0][0] is None:
                raise UnknownShape(
                    "lingxing_purchase_order_list_items 一个采集日都没有 —— "
                    "把采集缺口读成「没有在途」会让计划看起来不缺货")
            self._purchase_as_of = rows[0][0]
        return self._purchase_as_of

    def _in_transit(self) -> dict[str, list[InTransit]]:
        as_of = self.purchase_as_of()
        if self._transit_cache is not None and self._transit_cache[0] == as_of:
            return self._transit_cache[1]
        rows = self._q(SQL_PURCHASE_IN_TRANSIT.format(purchase_as_of=as_of.isoformat()))
        by_sku, dropped = parse_in_transit(rows)
        total = sum(t.units for v in by_sku.values() for t in v)
        # ★ 实测开口在途 28,561 件里 2026-10 只有 345 件、之后为 0 —— 计划窗口内
        #   几乎一件都不落。这是数据的形状，不是「没取到」，所以总量要打出来。
        log.info("op=ch_in_transit purchase_as_of=%s skus=%d units=%d dropped=%s",
                 as_of, len(by_sku), total, dropped)
        if dropped:
            log.warning("op=ch_in_transit outcome=partial dropped=%s —— "
                        "这些行没有预计到货日，已丢弃", dropped)
        self._transit_cache = (as_of, by_sku)
        self._stats["in_transit"] = dropped
        return by_sku

    def purchase_in_transit(self, sku: str) -> list[InTransit]:
        return list(self._in_transit().get(sku, ()))
```

`__init__` 补 `self._purchase_as_of: dt.date | None = None` 与 `self._transit_cache = None`。

- [ ] **Step 4: 跑测试，确认绿**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast.py tests/test_layering.py -rs`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add dim/ch_source.py shared/config.py config.example.toml tests/test_dim_ch_source_forecast.py
git commit -m "feat(dim): purchase_in_transit 按状态过滤 + 陈旧阈值(OQ-5) —— 已完成单 receive=0 会多算 3.7 倍，采集断档报未知不报 0"
```

---

### Task 4: monthly_sales_history —— 去重 · store→sid 映射 · 只取完整月

**Files:**
- Create: `dim/order_store_map.py`
- Modify: `dim/ch_source.py`
- Test: `tests/test_dim_ch_source_forecast.py`
- Test: `tests/test_order_store_map.py`（新建）

**Interfaces:**
- Consumes: Task 1 的 `ChSource`
- Produces:
  - `dim.order_store_map.STORE_SID: dict[tuple[str, str], str]` —— 17 组 `(store, sales_channel) -> sid`
  - `dim.order_store_map.EXCLUDED_CHANNELS: tuple[str, ...]` —— 显式排除的渠道前缀
  - `dim.order_store_map.sid_for(store: str, channel: str) -> str | None`
  - `dim.order_store_map.store_for(sid: str) -> tuple[str, str]` —— 反查，认不出抛 `UnknownStore`
  - `dim.order_store_map.UnknownStore(Exception)`
  - `SQL_MONTHLY_SALES: str`
  - `month_window(as_of: dt.date, months: int) -> list[dt.date]`
  - `fill_months(window, hits) -> tuple[list[tuple[dt.date, int]], int]` —— 返回 (补齐后的序列, 补 0 的月份数)
  - `ChSource.monthly_sales_history(self, seller_sku, sid, months) -> list[tuple[dt.date, int]]`
  - `ChSource.history_window(self) -> dict` —— 最近一次调用的窗口留痕，Task 5 写进 claim 响应

### ★ 控制器裁定追加（09-23，见 spec §8 OQ-1，与「两条改数的发现」一起裁定）

本任务下面的实现原方案**已经是**控制器要的形状，三点不用改，只补引用：
- **先去重再算**（按 `(amazon_order_id, order_item_id)`，`argMax(_captured_date)`）——裁定原话
  「订单行跨采集重复（8 月虚高 12.5%、9 月 19.4%）必须先去重」，即 `SQL_MONTHLY_SALES` 的
  `GROUP BY amazon_order_id, order_item_id` 那一步，**这是先决条件，不是可选优化**。
- **只统计完整自然月**（`month_window` 排除当月）——裁定原话「不猜补偿。只取完整月」。
- **口径标记随数走**（`history_window` 带 `zero_filled` / `newest_month_age_days` / `lag_note`）——
  裁定原话「UI 必须能说出『近 N 天可能偏低』——一个低 23% 却不带标记的数字比没有数字更坏」。
  ★ `lag_note` 的取值不许是可有可无的装饰字符串：Task 5 必须把它原样透传进
  `POST /plans/{id}/claims` 响应，**不许在装配层丢弃或改写**。
- L-3 的真·晚到订单占比仍**留阶段 B 实调**（OQ-1 未解部分）——本任务不需要、也不许去分离它。

- [ ] **Step 1: 写失败的测试**

`tests/test_order_store_map.py`：

```python
"""(store, sales_channel) → sid 的字面映射。★ 不解析店名，也不靠「msku 唯一」。

★ 为什么必须有这张表：`amazon_sp_api_report_all_orders` **没有 sid 列**，
  只有 `store`；而全库没有任何一张表同时带 `store` 与 `sid`（2026-09-23 实测）。
★ 为什么不用「marketplace + msku 唯一」这条近路：美国四个 sid 共 1,589 个 msku，
  实测仍有 2 个跨 sid（DHWC007036Y1Z2B、DHWC007036N1Z2B，均 11072∩11098）——
  0.13% 的静默错账，而静默错账正是本仓踩过六次的那一类。
"""
import pytest

from dim import order_store_map as m


def test_the_seventeen_amazon_pairs_are_all_there():
    assert len(m.STORE_SID) == 17
    assert m.sid_for("PETSFIT_NORTH_AMERICA", "Amazon.com") == "11072"
    assert m.sid_for("A4PET_NORTH_AMERICA", "Amazon.com") == "11093"
    assert m.sid_for("A4PET_EUROPE", "Amazon.co.uk") == "11094"


def test_non_amazon_channels_are_excluded_explicitly_not_by_accident():
    assert m.sid_for("PETSFIT_NORTH_AMERICA", "Non-Amazon US") is None
    assert m.sid_for("PETSFIT_EUROPE", "Non-Amazon DE") is None


def test_an_unmapped_amazon_pair_hard_fails():
    """★ A4PET_EUROPE 的 es/fr/ie/it/pl/com.be 近 90 天有 21 单却没有对应 sid
    （A4Pet 欧洲只有 UK 11094 与 DE 11095）—— 是真没开店还是 seller_list 缺行，
    没查清（design §8 OQ-3）。没查清就硬失败，不猜。"""
    with pytest.raises(m.UnknownStore, match="A4PET_EUROPE.*Amazon.fr"):
        m.sid_for_or_raise("A4PET_EUROPE", "Amazon.fr")


def test_reverse_lookup_is_one_to_one():
    assert m.store_for("11072") == ("PETSFIT_NORTH_AMERICA", "Amazon.com")
    with pytest.raises(m.UnknownStore, match="11100"):
        m.store_for("11100")     # A4Pet-BS-JP-JP：SP-API 采集里根本没有它的 store
```

追加到 `tests/test_dim_ch_source_forecast.py`：

```python
def test_month_window_never_includes_the_current_month():
    """★ 当月是半个月 —— 实测 2026-09 只有 9,130 件 vs 8 月 11,906 件，
    混进去会被读成 23% 的下滑。"""
    got = cs.month_window(dt.date(2026, 9, 23), 3)
    assert got == [dt.date(2026, 6, 1), dt.date(2026, 7, 1), dt.date(2026, 8, 1)]


def test_interior_empty_months_are_filled_with_zero_and_counted():
    """★ 补 0 是为了 monthly_estimate 不抛 HistoryGap（forecast/estimate.py:57），
    而「这个月一件没卖」与「这个月没数据」是两件事 —— 后者不许补，要截短窗口。"""
    window = [dt.date(2026, 6, 1), dt.date(2026, 7, 1), dt.date(2026, 8, 1)]
    series, filled = cs.fill_months(window, {dt.date(2026, 6, 1): 561,
                                             dt.date(2026, 8, 1): 318})
    assert series == [(dt.date(2026, 6, 1), 561), (dt.date(2026, 7, 1), 0),
                      (dt.date(2026, 8, 1), 318)]
    assert filled == 1


def test_no_rows_at_all_returns_empty_not_zeros():
    """★ 协议原话：没有行就返回空列表 —— 不补 0（dim/source.py:25）。
    新品没数据与卖了 0 件必须分得开（forecast/estimate.py:22）。"""
    series, filled = cs.fill_months([dt.date(2026, 8, 1)], {})
    assert series == []
    assert filled == 0


def test_sql_dedupes_by_order_item():
    """★ 实测 8,980 组 (amazon_order_id, order_item_id) 出现两次，跨采集日状态会变
    （Pending → Shipped）。不去重时 2026-08 是 13,611 件、去重后 11,906（−12.5%），
    2026-09 是 11,331 → 9,130（−19.4%）。兄弟仓那条 SQL 没有这一步。"""
    sql = " ".join(cs.SQL_MONTHLY_SALES.split())
    assert "GROUP BY amazon_order_id, order_item_id" in sql
    assert "argMax(order_status" in sql and "argMax(quantity" in sql
    assert "'Cancelled'" in sql and "'Pending'" in sql
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_order_store_map.py tests/test_dim_ch_source_forecast.py -k "month or sales or store" -rs`
Expected: FAIL，`ModuleNotFoundError: No module named 'dim.order_store_map'`。

- [ ] **Step 3: 写实现**

`dim/order_store_map.py`：

```python
"""(store, sales_channel) → sid。★ 这是一张**声明**的表，不是解析出来的。

★ 为什么需要它：`amazon_sp_api_report_all_orders` 没有 `sid` 列，只有 `store`；
  而 jxd_raw 里**没有任何一张表同时带 `store` 与 `sid`**（2026-09-23 实测）。
  桥只能自己声明 —— 而声明的表必须有守卫盯着它别过期（tests/test_order_store_map_live.py）。

★ 为什么不解析店名：`PETSFIT_NORTH_AMERICA` 要拆成「账号族 + 区域」，
  而 `lingxing_seller_list.account_name`（PETSFIT-JXD-US）与它并不同形。
  名字会被改写，id 不会（同 CLAUDE.md 对仓名的约定）。

★ 为什么不用「marketplace + msku 唯一」：美国四个 sid 共 1,589 个 msku，
  实测仍有 2 个跨 sid —— 0.13% 的静默错账。
"""
from __future__ import annotations


class UnknownStore(Exception):
    """认不出的 (store, channel) 或 sid。★ 硬失败 —— 猜一个 sid 就是把一家店的
    销量记到另一家头上，而两边各自都「没报错」。"""


#: 2026-09-23 实测：近 90 天出现过 37 组 (store, sales_channel)，Amazon 渠道 23 组，
#: 其中这 17 组能对到 sid。另外 6 组（A4PET_EUROPE 的 es/fr/ie/it/pl/com.be，
#: 共 21 单）在 lingxing_seller_list 里没有对应 sid —— 见 design §8 OQ-3，硬失败。
STORE_SID: dict[tuple[str, str], str] = {
    ("PETSFIT_NORTH_AMERICA", "Amazon.com"):    "11072",
    ("PETSFIT_NORTH_AMERICA", "Amazon.ca"):     "11073",
    ("PETSFIT_NORTH_AMERICA", "Amazon.com.mx"): "11075",
    ("PETSFIT_NORTH_AMERICA", "Amazon.com.br"): "11076",
    ("PETSFIT_EUROPE", "Amazon.co.uk"):         "11077",
    ("PETSFIT_EUROPE", "Amazon.de"):            "11078",
    ("PETSFIT_EUROPE", "Amazon.fr"):            "11079",
    ("PETSFIT_EUROPE", "Amazon.it"):            "11085",
    ("PETSFIT_EUROPE", "Amazon.pl"):            "11086",
    ("PETSFIT_EUROPE", "Amazon.es"):            "11087",
    ("PETSFIT_EUROPE", "Amazon.se"):            "11088",
    ("PETSFIT_EUROPE", "Amazon.com.be"):        "11089",
    ("PETSFIT_EUROPE", "Amazon.nl"):            "11090",
    ("PETSFIT_EUROPE", "Amazon.ie"):            "11091",
    ("PETSFIT_ASIA",   "Amazon.co.jp"):         "11092",
    ("A4PET_NORTH_AMERICA", "Amazon.com"):      "11093",
    ("A4PET_EUROPE", "Amazon.co.uk"):           "11094",
}

#: ★ 显式排除并计数，不是「碰巧没映射上」。这些是走同一张报表的非 Amazon 渠道
#:   （Walmart / Chewy 等），不是这个 Amazon 店的需求。近 90 天 14 组。
EXCLUDED_CHANNELS = ("Non-Amazon",)

#: ★ A4PET_EUROPE 的这几个渠道近 90 天有单但没有 sid（OQ-3 未查清）。
#:   放进这里只是为了让 sid_for() 返回 None 而不是抛 —— 但 sid_for_or_raise()
#:   仍然抛，调用方按自己的位置选。**不许把它们悄悄并进 EXCLUDED_CHANNELS。**
UNRESOLVED: frozenset[tuple[str, str]] = frozenset()

_SID_STORE = {v: k for k, v in STORE_SID.items()}
assert len(_SID_STORE) == len(STORE_SID), "两组 (store, channel) 指向了同一个 sid"


def _excluded(channel: str) -> bool:
    return any(channel.startswith(p) for p in EXCLUDED_CHANNELS)


def sid_for(store: str, channel: str) -> str | None:
    if _excluded(channel):
        return None
    return STORE_SID.get((store, channel))


def sid_for_or_raise(store: str, channel: str) -> str:
    sid = sid_for(store, channel)
    if sid is None:
        raise UnknownStore(
            f"(store={store!r}, sales_channel={channel!r}) 不在映射表里。"
            "★ 不猜 —— 猜一个 sid 就是把一家店的销量记到另一家头上。"
            "新开的店请补进 dim/order_store_map.py::STORE_SID")
    return sid


def store_for(sid: str) -> tuple[str, str]:
    if str(sid) not in _SID_STORE:
        raise UnknownStore(
            f"sid={sid!r} 在订单报表里没有对应的 store —— "
            "这个店的销量取不到，不能当成「卖了 0 件」")
    return _SID_STORE[str(sid)]
```

`dim/ch_source.py` 追加：

```python
#: ★ 三个口径决定，每一个都是实测踩出来的：
#:   ① 按 (amazon_order_id, order_item_id) 去重，取 argMax(_captured_date) 的那一版。
#:      实测 8,980 组出现两次，跨采集日状态会变（Pending → Shipped）。不去重时
#:      2026-08 = 13,611 件、去重后 11,906（−12.5%），2026-09 = 11,331 → 9,130（−19.4%）。
#:      去重后与独立源 lingxing_multiplatform_sales_stats 对上（2026-06 差 0.06%）。
#:      ★ 兄弟仓的 [demand_history] 没有这一步 —— 这是本仓实测追加的。
#:   ② 排除 Cancelled / Pending（沿用兄弟仓口径）。实测 Pending 在已完结的月份
#:      残留很少（2026-07 为 165/25,772）。
#:   ③ 只统计一个 (store, sales_channel) —— sid 由 dim/order_store_map.py 声明，
#:      不在 SQL 里解析店名。
SQL_MONTHLY_SALES = """
SELECT toStartOfMonth(d) AS month, toInt64(sum(units)) AS units
  FROM (
    SELECT toDate(any(purchase_date)) AS d,
           if(argMax(order_status, _captured_date) NOT IN ('Cancelled', 'Pending'),
              argMax(quantity, _captured_date), 0) AS units
      FROM jxd_raw.amazon_sp_api_report_all_orders
     WHERE store = '{store}' AND sales_channel = '{channel}' AND sku = '{seller_sku}'
       AND toDate(purchase_date) >= toDate('{start}')
       AND toDate(purchase_date) < toDate('{end}')
     GROUP BY amazon_order_id, order_item_id
  )
 GROUP BY month
 ORDER BY month
"""


def month_window(as_of: dt.date, months: int) -> list[dt.date]:
    """最近 `months` 个**完整**自然月，升序。★ 当月绝不入选 ——
    实测 2026-09 只有 9,130 件 vs 8 月 11,906 件，混进去会被读成 23% 的下滑。"""
    if not isinstance(months, int) or isinstance(months, bool) or months < 1:
        raise ValueError(f"months 必须是正整数，收到 {months!r}")
    out: list[dt.date] = []
    y, m = as_of.year, as_of.month
    for _ in range(months):
        m -= 1
        if m == 0:
            y, m = y - 1, 12
        out.append(dt.date(y, m, 1))
    return sorted(out)


def fill_months(window: list[dt.date],
                hits: dict[dt.date, int]) -> tuple[list[tuple[dt.date, int]], int]:
    """窗口内空月补 0 并计数。★ 一条命中都没有 → 返回 `[]`（协议：不补 0）——
    「新品没数据」与「卖了 0 件」必须分得开（forecast/estimate.py:22）。"""
    if not hits:
        return [], 0
    series = [(m, int(hits.get(m, 0))) for m in window]
    return series, sum(1 for m in window if m not in hits)
```

`ChSource` 方法：

```python
    def monthly_sales_history(self, seller_sku: str, sid: str, months: int
                              ) -> list[tuple[dt.date, int]]:
        store, channel = order_store_map.store_for(str(sid))
        window = month_window(self.as_of(), months)
        start, end = window[0], _next_month(window[-1])
        rows = self._q(SQL_MONTHLY_SALES.format(
            store=store, channel=channel, seller_sku=seller_sku,
            start=start.isoformat(), end=end.isoformat()))
        hits = {r[0]: int(r[1]) for r in rows}
        extra = sorted(m.isoformat() for m in hits if m not in set(window))
        if extra:
            raise UnknownShape(
                f"月销量落在窗口之外：{extra}。静默忽略就是漏了一段历史")
        series, filled = fill_months(window, hits)
        newest_age = (self.as_of() - window[-1]).days
        self._history_window = {
            "store": store, "sales_channel": channel,
            "months": [m.isoformat() for m in window],
            "zero_filled": [m.isoformat() for m in window if m not in hits],
            "newest_month": window[-1].isoformat(),
            "newest_month_age_days": newest_age,
            # ★ L-3：订单是滞后采集的。去重已经消掉「重复采集」那一成因，
            #   残余的「真·晚到订单」本期未测（design §8 OQ-1）—— 所以把口径
            #   说出去，而不是给一个没有标记的数。
            "lag_note": "orders_are_lagging_collected; deduped by order_item",
        }
        log.info("op=ch_sales store=%s channel=%s msku=%s months=%d hits=%d"
                 " zero_filled=%d newest_age_days=%d",
                 store, channel, seller_sku, months, len(hits), filled, newest_age)
        return series

    def history_window(self) -> dict:
        return dict(self._history_window)
```

补 `from dim import order_store_map`、`self._history_window: dict = {}`，以及模块级
`def _next_month(d): return dt.date(d.year + d.month // 12, d.month % 12 + 1, 1)`。

- [ ] **Step 4: 跑测试，确认绿**

Run: `uv run pytest -q tests/test_order_store_map.py tests/test_dim_ch_source_forecast.py tests/test_layering.py -rs`
Expected: PASS。`test_layering.py` 必须仍绿 —— `dim/order_store_map.py` 只 import 标准库。

- [ ] **Step 5: 提交**

```bash
git add dim/order_store_map.py dim/ch_source.py tests/test_order_store_map.py tests/test_dim_ch_source_forecast.py
git commit -m "feat(dim): monthly_sales_history —— 按 order_item 去重（近月差 12~19%）、声明 store→sid、只取完整月"
```

---

### Task 5: 装配 —— 配置开关 · 惰性建连 · 503 翻译 · claim 带上历史窗口

**Files:**
- Create: `api/ui/source_factory.py`
- Modify: `api/ui/plans.py:32`（换成 `SOURCE = make_source()`）、`api/ui/plans.py` 的 `claim()` 与 `grid()`
  （新增 `_source_error()` 翻译函数与 try/except，不碰 `api/__init__.py`——见下方裁定追加）
- Test: `tests/test_api_forecast_source.py`（新建）

**Interfaces:**
- Consumes: Task 1~4 的 `ChSource`、`ChUnavailable`、`ChDataUnusable`、`UnknownShape`、`PurchaseTableStale`（Task 3）、`ChSource.stats()`、`ChSource.history_window()`；既有 `shared.ch_client.ch_client/ch_query/describe_failure`、`api.ui.errors.ApiError`
- Produces:
  - `api.ui.source_factory.make_source() -> Source`
  - `api.ui.source_factory.lazy_ch_query() -> Query` —— 首次调用时才建连并缓存
  - `api.ui.plans._source_error(e: Exception) -> ApiError` —— 把 ChSource 的四种失败翻成
    `ApiError`；认不出的异常原样往外冒，不兜底
  - `claim()` 响应新增键 `history_window: dict | None`
  - `grid()` 响应新增键 `source_notes: dict`（`{"as_of", "purchase_as_of", "dropped": {...}}`）
  - `grid()` 的 `sku_pipeline[]` 每个槽位新增键 `bucket: "overdue" | "future"`（Task 3 OQ-6 裁定）
  - 错误码 `forecast_source_unavailable`（503）、`forecast_source_unusable`（503）

### ★ 控制器裁定追加（09-23，见 spec §6「失败行为」与 team-lead 对本任务的复核）

- **不新发明错误处理机制，照抄房子既有形状**：本仓已有的模式是
  `jobs/refresh_dims.ChUnreachable` 在**调用方**（`api/ui/ops.py::refresh_dims_now`）被
  `except` 住、翻成 `raise ApiError(503, "refresh_failed", ..., {...}) from e`，由
  `api/__init__.py` 里**唯一**一个通用的 `@app.exception_handler(ApiError)` 兜底渲染成
  `{"error", "hint", **fields}`。原草案给 `ChUnavailable` / `ChDataUnusable` / `UnknownShape`
  各注册一个 `@app.exception_handler`，是**另发明了一条**处理路径——两条路径并存，
  下次给第五种异常时无法判断该走哪条。**改为**：`api/ui/plans.py` 里写一个
  `_source_error(e) -> ApiError` 翻译函数，`grid()` 与 `claim()` 各用一个 try/except
  把 `SOURCE.*` 调用包起来，`raise _source_error(e) from e`；`api/__init__.py`
  **不需要任何改动**（已有的 `ApiError` 处理器直接生效）。
- **`PurchaseTableStale`（Task 3 新增）也在 `_source_error` 里翻译**：503
  `forecast_source_unusable`，带 `captured` / `age_days` / `threshold_days`——采购表陈旧
  与「掉档守卫全拒」是同一类失败（数据不可用，不是没生意），共用一个错误码，不再单起一个。
- **`sku_pipeline` 补 `bucket` 字段**（OQ-6）：组装 `pipeline[(sku, t.period)]` 时，
  用 `cs.is_overdue(t.period, SOURCE.purchase_as_of())` 判定，写
  `"bucket": "overdue" if cs.is_overdue(...) else "future"`。★ 这一步只在 `SOURCE` 具备
  `purchase_as_of`（即 CH 源）时才有意义；`FixtureSource` 没有 `purchase_as_of`，按现有
  `hasattr` 判断跳过——fixture 路径的 `sku_pipeline` 形状因此**必须逐字节不变**
  （下面 Step 4 用 `tests/test_grid_fixture.py` 核实）。

- [ ] **Step 1: 写失败的测试**

`tests/test_api_forecast_source.py`：

```python
"""装配：配置开关 · 惰性建连 · 取不到数一律 503。"""
from __future__ import annotations

import datetime as dt

import pytest

from api.ui import source_factory as sf
from dim import ch_source as cs
from dim.fixture_source import FixtureSource


def test_default_source_is_fixture(monkeypatch):
    """★ 默认值必须是离线跑得通的那个 —— 漏配不该变成「测试去连生产 CH」。"""
    monkeypatch.setattr(sf, "forecast", lambda: {"source": "fixture"})
    assert isinstance(sf.make_source(), FixtureSource)


def test_ch_source_does_not_connect_at_construction(monkeypatch):
    """★ import 时建连会让「配置写错」在启动前一秒才炸，而那时日志已经刷过去了。"""
    built = []
    monkeypatch.setattr(sf, "forecast", lambda: {"source": "ch", "cache_ttl_seconds": 300,
                                                  "drop_threshold": 0.30})
    monkeypatch.setattr(sf, "ch_client", lambda: built.append(1) or object())
    monkeypatch.setattr(sf, "ch_query", lambda c: (lambda sql: []))
    src = sf.make_source()
    assert built == [], "构造时就连了 CH"
    with pytest.raises(Exception):
        src.as_of()
    assert built == [1], "第一次真查询时才该建连"


def test_unknown_source_value_hard_fails(monkeypatch):
    monkeypatch.setattr(sf, "forecast", lambda: {"source": "excel"})
    with pytest.raises(ValueError, match="excel"):
        sf.make_source()


def test_ch_unavailable_becomes_503_naming_the_kind(client, seed, monkeypatch):
    """★ 与 mirror_stale 同一条理由：拿不到数算出来的曲线看起来完全正常。
    调度器必须能区分「旧」「无」「不适用」。"""
    from api.ui import plans

    class Dead:
        def as_of(self):
            raise cs.ChUnavailable("192.168.66.211:8123/jxd_raw",
                                   {"kind": "connect_refused", "code": 111})
        def monthly_sales_history(self, *a):
            raise cs.ChUnavailable("x", {"kind": "timeout"})
        def onhand_available(self, *a):
            raise cs.ChUnavailable("x", {"kind": "timeout"})
        def purchase_in_transit(self, *a):
            raise cs.ChUnavailable("x", {"kind": "timeout"})

    monkeypatch.setattr(plans, "SOURCE", Dead())
    plan = client.post("/v1/plans", json={"title": "t", "period_start": "2026-10-01",
                                          "months": 3}, headers={"x-actor": seed.actor}).json()
    r = client.get(f"/v1/plans/{plan['plan_id']}/grid")
    assert r.status_code == 503
    body = r.json()
    assert body["error"] == "forecast_source_unavailable"
    assert body["kind"] == "connect_refused", "只说「失败了」等于没说 —— 超时与连不上处置相反"
    assert body["target"] == "192.168.66.211:8123/jxd_raw"


def test_claim_returns_the_history_window(client, seed, monkeypatch):
    """★ 没有标记的数比没有数更坏：用了哪几个月、哪些是补 0，界面要说得出来。"""
    from api.ui import plans

    class Src(FixtureSource):
        def history_window(self):
            return {"store": "PETSFIT_NORTH_AMERICA", "sales_channel": "Amazon.com",
                    "months": ["2026-07-01", "2026-08-01"], "zero_filled": ["2026-07-01"],
                    "newest_month": "2026-08-01", "newest_month_age_days": 23,
                    "lag_note": "orders_are_lagging_collected; deduped by order_item"}

    monkeypatch.setattr(plans, "SOURCE", Src(plans.FIXTURES))
    plan = client.post("/v1/plans", json={"title": "t", "period_start": "2026-10-01",
                                          "months": 2}, headers={"x-actor": seed.actor}).json()
    r = client.post(f"/v1/plans/{plan['plan_id']}/claims",
                    json={"seller_sku": seed.msku_a[0], "sid": seed.msku_a[1]},
                    headers={"x-actor": seed.actor})
    assert r.status_code == 200
    assert r.json()["history_window"]["zero_filled"] == ["2026-07-01"]
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_api_forecast_source.py -rs`
Expected: FAIL，`ModuleNotFoundError: No module named 'api.ui.source_factory'`。

- [ ] **Step 3: 写实现**

`api/ui/source_factory.py`：

```python
"""装配取数源。★ 这一层允许 import shared.ch_client —— dim/ 不允许
（tests/test_layering.py:55 的 NO_CONNECTIONS），所以注入点必须在 api/。"""
from __future__ import annotations

import logging
from collections.abc import Callable
from pathlib import Path

from dim.ch_source import ChSource
from dim.fixture_source import FixtureSource
from dim.source import Source
from shared.ch_client import ch_client, ch_query
from shared.config import forecast

log = logging.getLogger("scm.api")

FIXTURES = Path(__file__).resolve().parents[2] / "tests" / "fixtures"


def lazy_ch_query() -> Callable[[str], list[tuple]]:
    """★ 第一次真查询时才建连：import 时建连会让配置错在启动前一秒才炸，
    而那时启动日志已经刷过去了（CLAUDE.md 判据四的反面）。"""
    holder: list = []

    def run(sql: str) -> list[tuple]:
        if not holder:
            holder.append(ch_query(ch_client()))
        return holder[0](sql)

    return run


def make_source() -> Source:
    cfg = forecast()
    kind = cfg.get("source", "fixture")
    if kind == "fixture":
        log.info("op=forecast_source kind=fixture root=%s", FIXTURES)
        return FixtureSource(FIXTURES)
    if kind == "ch":
        log.info("op=forecast_source kind=ch ttl_s=%s threshold=%s",
                 cfg.get("cache_ttl_seconds"), cfg.get("drop_threshold"))
        return ChSource(lazy_ch_query(),
                        drop_threshold=float(cfg.get("drop_threshold", 0.30)),
                        cache_ttl_s=int(cfg.get("cache_ttl_seconds", 300)))
    # ★ 配置类错误往启动钩子放，别等第一个请求才炸。
    raise ValueError(f"[forecast] source 只认 'fixture' 与 'ch'，配的是 {kind!r}")
```

`api/ui/plans.py`：第 21 行的 `from dim.fixture_source import FixtureSource` 换成
`from api.ui.source_factory import FIXTURES, make_source`，第 32 行换成：

```python
#: 阶段 A 的取数源。★ 换源只改 config.toml 的 [forecast] source —— dim/source.py 的协议不变。
SOURCE = make_source()
```

`api/ui/plans.py` 顶部追加 `from dim.ch_source import (ChDataUnusable, ChUnavailable,
PurchaseTableStale, UnknownShape, is_overdue)`，并加一个翻译函数（★ 房子既有形状：照抄
`api/ui/ops.py::refresh_dims_now` 捕 `refresh_dims.ChUnreachable` 再 `raise ApiError(...)`
的路子，**不**给 `api/__init__.py` 新增任何 `@app.exception_handler`——那条路径本仓已经有且
只有一条，不重开第二条）：

```python
def _source_error(e: Exception) -> ApiError:
    """把 ChSource 的失败翻成 ApiError —— 同 jobs/refresh_dims.ChUnreachable → refresh_failed
    的形状，不另发明一种。★ 一律 503：拿不到数算出来的曲线看起来完全正常，S-29/S-30 的
    错误形状统一裁定同样适用于这条路径。"""
    if isinstance(e, ChUnavailable):
        return ApiError(503, "forecast_source_unavailable",
                        "预测取数源连不上，拒绝服务 —— 不拿旧数或空数冒充",
                        {"target": e.target, **e.cause})
    if isinstance(e, ChDataUnusable):
        return ApiError(503, "forecast_source_unusable",
                        "近 7 个采集日全都掉档或未采完 —— 这批数据不可用，不是「库存为 0」",
                        {"rejected": e.rejected})
    if isinstance(e, PurchaseTableStale):
        # ★ OQ-5 裁定：陈旧阈值突破 → 未知，不是 0；与「掉档守卫全拒」同一类失败，
        #   共用 forecast_source_unusable，不再单起一个错误码。
        return ApiError(503, "forecast_source_unusable",
                        "采购单快照太久没更新 —— 在途视为未知，不是 0",
                        {"captured": e.captured.isoformat(), "age_days": e.age_days,
                         "threshold_days": e.threshold_days})
    if isinstance(e, UnknownShape):
        return ApiError(503, "forecast_source_unusable", "取数源出现认不出的形态",
                        {"detail": str(e)})
    raise e   # ★ 认不出的异常不许被兜成这四种之一——同 CLAUDE.md「不许 else 兜底」
```

`claim()` 里把 `history_window` 带上，并把取数包进翻译：

```python
        no_history = []
        history_window = None
        try:
            est = monthly_estimate(SOURCE.monthly_sales_history(seller_sku, sid, len(periods)),
                                   len(periods))
        except InsufficientHistory:
            est = None
            no_history.append({"seller_sku": seller_sku, "sid": sid,
                               "reason": "no_sales_history"})
        except (ChUnavailable, ChDataUnusable, UnknownShape) as e:
            raise _source_error(e) from e
        # ★ 用了哪几个月、哪些是补 0、最新那个月距今多少天 —— 订单是滞后采集的
        #   （CLAUDE.md 铁律三），一个没有标记的数比没有数更坏。
        if hasattr(SOURCE, "history_window"):
            history_window = SOURCE.history_window() or None
```

返回值追加 `"history_window": history_window`。

`grid()` 里把现有对 `SOURCE.as_of()` / `SOURCE.onhand_available()` / `SOURCE.purchase_in_transit()`
的调用整段包进一个 try/except（这几处已经在同一个请求处理函数体内，不必分散包）：

```python
    try:
        as_of = SOURCE.as_of()
        ...                      # 原有的 onhand_available 循环、purchase_in_transit 字典推导式不变
    except (ChUnavailable, ChDataUnusable, PurchaseTableStale, UnknownShape) as e:
        raise _source_error(e) from e
```

`pipeline` 槽位新增 `bucket`（OQ-6 裁定，只在 CH 源下有意义）：

```python
    purchase_as_of = SOURCE.purchase_as_of() if hasattr(SOURCE, "purchase_as_of") else None
    pipeline: dict[tuple[str, str], dict] = {}
    for sku, rows in sorted(in_transit.items()):
        for t in rows:
            slot = pipeline.setdefault((sku, t.period),
                                       {"sku": sku, "period": t.period, "units": 0,
                                        "sources": [], "no_seller_attribution": True,
                                        # ★ E-13 实测 98.8% 的在途会落进 overdue——这是数据的
                                        #   形状，不是 bug，但不许因为不匹配任何计划月份就
                                        #   从 sku_pipeline 里悄悄消失。
                                        "bucket": (None if purchase_as_of is None else
                                                  ("overdue" if is_overdue(t.period, purchase_as_of)
                                                   else "future"))})
            slot["units"] += t.units
            slot["sources"].append({"units": t.units, "kind": "purchase_in_transit", "ref": t.ref})
```

`grid()` 返回值追加：

```python
        "source_notes": {
            "as_of": as_of,
            "purchase_as_of": (purchase_as_of.isoformat() if purchase_as_of else None),
            # ★ 被排除的那一侧必须出现在响应里：sid=0 的欧洲共享池实测占全部可售
            #   27.6%（键名固定 shared_pool_excluded，Task 2 裁定），没有在途预计到货日的
            #   行也在这里 —— 丢东西必须有声。
            "dropped": SOURCE.stats() if hasattr(SOURCE, "stats") else {},
        },
```

- [ ] **Step 4: 跑全部测试**

Run: `uv run pytest -q -rs` 然后 `uv run ruff check .`
Expected: 全绿。★ `tests/test_grid_fixture.py`、`tests/test_api_grid.py` 必须**一条都不改**仍绿 —— 它们跑在 `[forecast] source = "fixture"` 下。若它们红，说明装配改动泄漏到了 fixture 路径上（`purchase_as_of` 为 `None` 时 `bucket` 也必须是 `None`，不是猜一个）。

- [ ] **Step 5: 提交**

```bash
git add api/ui/source_factory.py api/ui/plans.py tests/test_api_forecast_source.py
git commit -m "feat(api): 按 [forecast] source 装配取数源；取不到数一律 503 走既有 ApiError 形状，不新开处理路径"
```

---

### Task 6: 实测门禁 —— 真连 CH 跑一次 + 逐格对账 + 映射表不许过期

**Files:**
- Create: `tests/test_dim_ch_source_forecast_live.py`
- Create: `tests/test_order_store_map_live.py`
- Modify: `docs/00e-分阶段实施计划.md`（阶段 A「预测」行注明取数已接真 CH）
- Modify: `README.md`（`[forecast]` 段与换源方法）

**Interfaces:**
- Consumes: Task 1~5 的全部产出；既有 `shared.ch_client.ch_client/ch_query/describe_failure`
- Produces: 无新接口；两个 live 测试文件

### ★ 控制器裁定追加（09-23）

- 本任务的两条既有门禁**已经是**控制器要的形状，不用改：`test_monthly_sales_matches_the_frozen_
  hand_computed_months` 挑真实 msku（`DCC1800264G1Z2B` / sid `11072`）跟另写的手算 SQL 对账，
  `test_every_amazon_pair_in_ch_is_declared` 让 `dim/order_store_map.py` 的映射表在 CH 长出新
  `(store, sales_channel)` 时红。这两条分别覆盖裁定要求的「真值对账挑真实 msku」与「查找表要有
  过期门禁」，Step 3 里 OQ-3 的处理方式（补 `STORE_SID` 或写进 `UNRESOLVED` 并显式豁免、
  不许并进 `EXCLUDED_CHANNELS`）同样照原方案执行。
- 追加一条同类门禁：Task 3 新增的 `purchase_staleness_days` 阈值也要有真实数据核实——
  在 `tests/test_dim_ch_source_forecast_live.py` 里加一条 `test_purchase_as_of_is_within_staleness_
  budget`，读 `live_source.purchase_as_of()` 与 `live_source.as_of()` 的天数差，断言
  `< purchase_staleness_days`；若这台机器上真的超了，**测试应该红**——红了说明默认值 3 天
  定小了或者采集真的断了，两种都不该被静默吞掉。

- [ ] **Step 1: 写失败的测试**

`tests/test_dim_ch_source_forecast_live.py`：

```python
"""实测：四个方法在真 ClickHouse 上真的跑得通，且算出来的数与手算一致。

★ 存在的理由（同 tests/test_dim_ch_source_live.py:3-12）：纯变换测试的假 query
  完全无视 SQL 文本 —— SQL 里的列名错、类型不兼容（NO_COMMON_TYPE）、
  `argMax` 写错分组，一条都测不出来。
★ 只在 CH 不可达时跳过，且跳过原因必须点名 host/port/cause ——
  跳过与「没测到」必须长得不一样。这台机器上 CH 是可达的，本文件应当真的跑。
"""
from __future__ import annotations

import datetime as dt

import pytest

from dim import ch_source as cs
from shared.ch_client import ch_client, ch_query, describe_failure
from shared.config import clickhouse

#: ★ 对账靶子。2026-09-23 实测，去重后的**已完结**月份：
#:     2026-06 = 561 件   2026-07 = 254 件
#:   （去重前 2026-08 是 370、去重后 318 —— 8 月还在动，所以不拿它当靶子。）
#:   这两个数一旦变了，测试就该红：那说明「已完结的月份」其实还在长，
#:   而那正是 design §8 OQ-1 要测的东西。
RECON_MSKU, RECON_SID = "DCC1800264G1Z2B", "11072"
RECON_MONTHS = {dt.date(2026, 6, 1): 561, dt.date(2026, 7, 1): 254}


@pytest.fixture(scope="module")
def live_source():
    c = clickhouse()
    where = f"{c['host']}:{c.get('port', 8123)}/{c.get('database', 'jxd_raw')}"
    try:
        q = ch_query(ch_client())
        q("SELECT 1")
    except Exception as e:  # noqa: BLE001 - 建连失败的类型不可预知
        pytest.skip(f"CH 不可达 target={where} cause={describe_failure(e)} —— "
                    f"跳过而非静默：本条从不因为「没测到」而绿")
    return cs.ChSource(q)


def test_as_of_runs_and_is_a_recent_settled_day(live_source):
    got = live_source.as_of()
    age = (dt.date.today() - got).days
    assert 0 <= age <= 7, f"as_of={got} 落在 7 天窗口之外 —— 守卫把所有候选都拒了？"


def test_onhand_matches_a_hand_written_query(live_source):
    """★ 对账要**另写一条** SQL —— 同一条 SQL 自己对自己不算对账。"""
    as_of = live_source.as_of()
    raw = ch_query(ch_client())(
        f"SELECT afn_fulfillable_quantity FROM jxd_raw.lingxing_inventory_fba_detail"
        f" WHERE _captured_date = toDate('{as_of}') AND sid = {RECON_SID}"
        f"   AND seller_sku = '{RECON_MSKU}'")
    assert raw, f"{RECON_MSKU} 在 {as_of} 的快照里没有行 —— 换一个对账靶子"
    assert live_source.onhand_available(RECON_MSKU, RECON_SID) == int(raw[0][0])


def test_shared_pool_is_excluded_and_the_count_is_not_zero(live_source):
    """★ 排除了 27.6% 的可售，这个计数必须真的不为 0 —— 为 0 说明排除没生效，
    或者 SQL 已经在别处把它滤掉了（那样这条规则就是空转的）。"""
    live_source.onhand_available(RECON_MSKU, RECON_SID)
    dropped = live_source.stats()["onhand"]
    assert dropped["shared_pool_excluded"]["units"] > 0
    assert dropped["shared_pool_excluded"]["rows"] > 0


def test_monthly_sales_matches_the_frozen_hand_computed_months(live_source):
    series = dict(live_source.monthly_sales_history(RECON_MSKU, RECON_SID, 6))
    for month, expected in RECON_MONTHS.items():
        assert series.get(month) == expected, (
            f"{month:%Y-%m} 手算 {expected}，取数给 {series.get(month)} —— "
            "要么口径变了，要么这个已完结的月份还在长（OQ-1）")


def test_dedupe_actually_removes_something(live_source):
    """★ 去重必须真的有东西可去：实测 8,980 组重复，2026-08 差 12.5%。
    若两边相等，说明去重是空转的 —— 而空转与守住了长得一模一样。"""
    q = ch_query(ch_client())
    raw = q("""SELECT sum(quantity) FROM jxd_raw.amazon_sp_api_report_all_orders
      WHERE store='PETSFIT_NORTH_AMERICA' AND sales_channel='Amazon.com'
        AND order_status NOT IN ('Cancelled','Pending')
        AND toYYYYMM(toDate(purchase_date)) = 202608""")[0][0]
    dedup = q("""SELECT sum(u) FROM (SELECT
        if(argMax(order_status,_captured_date) NOT IN ('Cancelled','Pending'),
           argMax(quantity,_captured_date), 0) AS u
      FROM jxd_raw.amazon_sp_api_report_all_orders
      WHERE store='PETSFIT_NORTH_AMERICA' AND sales_channel='Amazon.com'
        AND toYYYYMM(toDate(purchase_date)) = 202608
      GROUP BY amazon_order_id, order_item_id)""")[0][0]
    assert dedup < raw, "去重一件都没去掉 —— 这条守卫在空转"


def test_in_transit_excludes_completed_orders(live_source):
    """★ E-12：已完成单的行项 quantity_receive 全为 0。若过滤没生效，
    在途总量会是真实开口量的 3.7 倍左右。"""
    q = ch_query(ch_client())
    as_of = live_source.purchase_as_of()
    naive = q(f"""SELECT sum(quantity_real - quantity_receive)
      FROM jxd_raw.lingxing_purchase_order_list_items
      WHERE _captured_date = toDate('{as_of}') AND ifNull(is_delete,0)=0
        AND quantity_real > quantity_receive""")[0][0]
    live_source.purchase_in_transit("DVCD105013L1")
    total = sum(t.units for v in live_source._in_transit().values() for t in v)
    assert total < naive * 0.5, (
        f"按状态过滤后 {total} 件，不过滤是 {naive} 件 —— 差得太小，过滤可能没生效")
```

`tests/test_order_store_map_live.py`：

```python
"""映射表不许过期。★ 手列的宇宙必然漏第 N+1 种 —— 所以宇宙要从源里推导出来比。"""
from __future__ import annotations

import pytest

from dim import order_store_map as m
from shared.ch_client import ch_client, ch_query, describe_failure
from shared.config import clickhouse

WINDOW_DAYS = 90


def test_every_amazon_pair_in_ch_is_declared():
    c = clickhouse()
    where = f"{c['host']}:{c.get('port', 8123)}/{c.get('database', 'jxd_raw')}"
    try:
        q = ch_query(ch_client())
    except Exception as e:  # noqa: BLE001
        pytest.skip(f"CH 不可达 target={where} cause={describe_failure(e)}")
    rows = q(f"""SELECT store, sales_channel, count()
      FROM jxd_raw.amazon_sp_api_report_all_orders
      WHERE toDate(purchase_date) >= today() - {WINDOW_DAYS}
      GROUP BY store, sales_channel""")
    assert rows, "近 90 天一单都没有 —— 这条规则是空转的"
    unmapped = [(s, ch, n) for s, ch, n in rows
                if not ch.startswith(m.EXCLUDED_CHANNELS)
                and (s, ch) not in m.STORE_SID]
    assert not unmapped, (
        f"这些 (store, sales_channel) 在 CH 里有单却没有 sid 映射：{unmapped}。"
        "新开的店要补进 dim/order_store_map.py；确认无 sid 的要写进 design OQ-3，"
        "★ 不许悄悄并进 EXCLUDED_CHANNELS —— 那会让「没开店」和「忘了配」长得一样")
```

- [ ] **Step 2: 跑它，确认是红的**

Run: `uv run pytest -q tests/test_dim_ch_source_forecast_live.py tests/test_order_store_map_live.py -rs`
Expected: 全部 FAIL 或 skip。★ **必须先确认它们不是 skip** —— 这台机器上 CH 可达，`-rs` 的 skip 报告里不该出现它们。若出现，先修连通性再继续，不许把 skip 当绿。
★ 预期的真失败：`test_every_amazon_pair_in_ch_is_declared` 会点名 A4PET_EUROPE 的 es/fr/ie/it/pl/com.be 那 6 组。这时按 design §8 OQ-3 处理：**先查 `lingxing_seller_list` 里到底有没有这些站点的 sid**，有就补进 `STORE_SID`，确实没有就在 `UNRESOLVED` 里点名并在 assert 里显式豁免 —— 豁免要带注释与单数计数，不许改成 `EXCLUDED_CHANNELS`。

- [ ] **Step 3: 按红态修实现**

按上一步查到的结果补 `STORE_SID` 或 `UNRESOLVED`，并把 `test_every_amazon_pair_in_ch_is_declared` 的 assert 改成 `unmapped 必须 ⊆ m.UNRESOLVED`。其余 live 测试若红，修的是 SQL 而不是断言。

- [ ] **Step 4: 跑全套，确认绿**

Run: `uv run pytest -q -rs`、`uv run ruff check .`、`uv run pytest -q tests/test_layering.py`
Expected: 全绿，且 `-rs` 的报告里**没有** `test_dim_ch_source_forecast_live.py` / `test_order_store_map_live.py` 的 skip。

- [ ] **Step 5: 文档落地 + 提交**

`docs/00e-分阶段实施计划.md` 阶段 A「预测」那一行追加：
「★ 取数已接真 CH（`docs/superpowers/specs/2026-09-23-chsource-design.md`）：在仓来自 `lingxing_inventory_fba_detail`、销量来自 `amazon_sp_api_report_all_orders`（按 order_item 去重）、采购在途按采购单 `status=2` 过滤；`[forecast] source` 在 `fixture` 与 `ch` 之间切换。」

`README.md` 新增一节，写明 `[forecast]` 七个键（含 Task 3 新增的 `purchase_staleness_days`）、
换源只改一行、以及三个已知缺口：`sid=0` 共享池不分摊只排除计数（OQ-2）、逾期在途归入
`sku_pipeline[].bucket="overdue"` 单列展示不重定日期（OQ-6）、L-3 晚到订单占比未与重复采集
分开（OQ-1）。

```bash
git add tests/test_dim_ch_source_forecast_live.py tests/test_order_store_map_live.py dim/order_store_map.py docs/00e-分阶段实施计划.md README.md
git commit -m "test(dim): 真 CH 冒烟 + 逐格对账 + 映射表过期门禁"
```

---

## Self-Review

**1. Spec coverage**

| spec 章节 | 落在哪个 Task |
|---|---|
| §3 逐查询不做镜像 · §3.2 客户端注入 | Task 1（`ChSource.__init__` 只收 `Query`）+ Task 5（`source_factory` 注入） |
| §3.1 PG 镜像 vs CH 的分工 | Task 2（缺行给 0，不跨库 join）+ Task 4（`store_for` 只按 sid） |
| §4.1 as_of 裁定 + 掉档守卫 | Task 1 |
| §4.2 月销量四个口径 + `history_window` | Task 4 + Task 5 |
| §4.3 在仓列裁定 · sid=0 · 0 vs 未知 | Task 2 |
| §4.4 采购状态过滤 · NULL ETA · `purchase_as_of` | Task 3 |
| §5 缓存与批量 | Task 1（as_of TTL）· Task 2/3/4（按快照日缓存） |
| §6 失败行为 503 | Task 5 |
| §7 与 FixtureSource 对账 | Task 5 Step 4（fixture 测试一条不改仍绿）+ Task 6（真值对账） |
| §8 开放问题 | Task 6 Step 2（OQ-3 在红态里就要处理）+ Step 5（OQ-2/OQ-6 写进 README，OQ-5 的阈值键写进 `[forecast]` 注释） |

**2. Placeholder scan**：无 TBD / TODO / 「类似 Task N」。每个代码步骤都给了可直接粘的实现，每个测试步骤都给了带真实断言的测试与预期的 RED 信息。

**3. Type consistency**：`Query = Callable[[str], list[tuple]]`（沿用 `dim/ch_source.py:53`）全程一致；`sid` 在 PG、`order_store_map`、`parse_onhand` 三处**一律是 `str`**（`SQL_FBA_ONHAND` 里 `toString(sid)`，`STORE_SID` 的值是字符串字面量，`onhand_available` 里 `str(sid)`）；`InTransit(period: str, units: int, ref: str)` 沿用 `dim/source.py:12-15` 未改；`月份` 在 `month_window` / `fill_months` / `monthly_sales_history` 三处都是 `dt.date` 的月初，与 `forecast/estimate.py:39-40` 的校验一致；`ChSource.stats()` 的返回在 Task 2 定义、Task 3 追加 `in_transit` 键、Task 5 读，键名一致。

**4. 控制器裁定复核（09-23 preflight）**：六条 OQ 的裁定已逐条落进对应 Task（见每个 Task 里的
「★ 控制器裁定追加」小节）——OQ-1/OQ-4 确认原方案已对，只补引用；OQ-2 把丢弃计数的键名钉死成
`shared_pool_excluded`（Task 2，并同步改了 Step 1/3 的示例代码）；OQ-3 维持硬失败，不改；
OQ-5 是**新增行为**：Task 3 加了 `purchase_staleness_days`（默认 3）与 `PurchaseTableStale`，
Task 6 加了对应真值门禁；OQ-6 是**新增行为**：Task 3 保持 `period` 不重定日期，Task 5 在装配层
补 `sku_pipeline[].bucket`。Task 5 的失败处理同时按 team-lead 复核意见收敛成一条路径——
不再给 `api/__init__.py` 新增 `@app.exception_handler`，改为 `plans.py` 内 `_source_error()` +
`raise ApiError(...)`，照抄 `api/ui/ops.py::refresh_dims_now` 已有的形状。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-09-23-chsource.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - 每个 Task 派一个新 subagent，Task 之间复核，迭代快

**2. Inline Execution** - 在当前会话里按 `superpowers:executing-plans` 分批执行，带检查点

**Which approach?**
