# 后端接口清单 — service_supplychain

> **这份文档要做什么**
>
> 三份清单：★ **`api/ui/`**（给本仓前端）· ★ **`api/pub/`**（经 services 给 jxd_ai_ops）·
> **我们要对接的**（领星 / CH / PM）。
>
> ★★ **判据：对外暴露结论，不暴露内表。**
> 把内部表原样开出去，jxd_ai_ops 就绑在我们的表结构上，我们再也改不动。
>
> | | 谁用 | 改动约束 |
> |---|---|---|
> | `api/ui/` | ★ 本仓前端 | ★ **随界面一起变**，不进契约，不许外部直连 |
> | `api/pub/` | jxd_ai_ops（经 services） | ★ **是给别人的承诺** —— 改动要走契约版本 |
>
> ★ 下面「我们提供的」那份默认属 `api/ui/`；进 `api/pub/` 的必须逐条标注，
> 并说明**它暴露的是哪个结论**。
>
> ★ 外部那份必须逐条标 **幂等键 · 回查接口 · 副作用等级** ——
> 缺任何一项，超时重试就是在赌。

| | |
|---|---|
| 版本 | v1 草案 |
| 日期 | 2026-09-16 |
| 状态 | **待确认** |
| 上游 | `05` 模块（usecase）· `09` 用例 · `04` 状态流转 |

---

## 0. 约定

| 项 | 约定 |
|---|---|
| 前缀 | ★ 业务路由 `/v1`（`service.toml` 的 `path_prefix = "/v1"`） |
| 健康 | ★ `GET /health` —— **不加前缀**，探活目标 = `base_url + health.path` |
| 鉴权 | `x-internal-key`；★ 未配 → **启动失败**，不是运行时 503 |
| 操作人 | `x-actor` 头，★ **只记录不校验**（权限在上层） |
| 店铺号 | ★ 全字段 **字符串** —— 18 位，JS 会精度丢失 |
| 未声明查询参数 | ★ **一律 400** —— 拼错的参数被静默忽略，返回的是「全量」而不是报错 |
| 错误形状 | `{"code": "...", "message": "...", "detail": {...}}`；★ `detail` **必须点名是哪几行** |
| 命名 | ★ 路径不用领星词汇：无 `inbound` / `shipment` / `allocation` |

### 0.1 状态码语义

| 码 | 用在 |
|---|---|
| 400 | 参数问题（缺字段、未声明参数、理由未填） |
| 404 | 对象不存在 |
| **409** | ★ **冲突与闸** —— 占用撞了、版本在流转、闸没过、水位变了 |
| 410 | 已废弃的接口 |
| 422 | 语义不合法（如非法状态迁移目标） |
| 503 | ★ 维度镜像陈旧 → **拒绝服务**（E-4） |

★ **闸不过用 409 而不是 400**：400 是「你写错了」，409 是「你没写错，但现在不行」。
前端要能区分 —— 一个该改表单，一个该刷新重来。

---

## 1. 我们提供的接口

### 1.1 ② 需求域

| 方法 | 路径 | 用例 | 关键点 |
|---|---|---|---|
| **GET** | **`/v1/catalog/skus`** | UC-O2 | ★ **G1 补**：运营「**选货号**」这一步。列可选货号 + 其下 msku + **占用情况**（被哪张计划占了） |
| POST | `/v1/plans` | UC-O1 | 标题 · 起始月（须月初）· 跨 1~12 月（默认 3） |
| GET | `/v1/plans` | UC-O1/P1 | ★ **G2 补**：支持 `?state=&owner=&archived=`；`state` 是**派生的整体状态** |
| POST | `/v1/plans/{plan_id}/claims` | UC-O2 | ★ 409 `msku_already_claimed` + **点名占用方** |
| DELETE | `/v1/plans/{plan_id}/claims/{seller_sku}/{sid}` | UC-O2 | 释放不删行 |
| GET | `/v1/plans/{plan_id}/cells` | UC-O3 | ★ 每格带 `msku_breakdown[]`（A-6 可展开） |
| PUT | `/v1/plans/{plan_id}/cells/{seller_id}/{sku}/{period}` | UC-O4 | ★ 一格一事务；重算在事务外 |
| POST | `/v1/plans/{plan_id}/submit` | UC-O5 | ★ 返回 `rev` + **`skipped[]` 逐条理由** |
| GET | `/v1/plans/{plan_id}/revs` | UC-O6 | 带 `in_flight_rev` / `current_rev` |
| POST | `/v1/plans/{plan_id}/revs/{rev}/current` | UC-O7 | 标记当前使用 |
| GET | `/v1/plans/{plan_id}/diff?from=&to=` | UC-O6 | 版本间变更 |
| POST | `/v1/plans/{plan_id}/archive` | — | 同一事务释放全部占用 |
| GET | `/v1/plan-lines` | UC-P1/P2 | ★ **G3 补**：`?plan_ids=1,2,3` **勾选多份联合查看**；另支持 `?state=&sku=&category=&period=&group_by=sku｜category｜period` |
| GET | `/v1/plan-lines/{line_id}/trace` | UC-O9/P7 | ★ **承重墙①②反查**：采购单 · 排货单 · 外部单号 |
| POST | `/v1/plan-lines/{line_id}/cancel` | UC-A1 | ★ **理由必填**，400 `reason_required` |
| POST | `/v1/plans/{plan_id}/revs/{rev}/cancel` | A-8 | ★ **逐条撤其全部未终态记录**，一个理由记在每条上 |
| GET | `/v1/dashboard/plans` | UC-O8 | ★ **G8 明确**：**计划级**计数 —— 进行中（未完结）· 已提交 · 已提交未确认 · 已下单 · 准备排货 · 已排货，各多少**张计划**。★ 计划的状态是**记录状态的木桶派生**，不是独立字段 |
| GET | `/v1/dashboard/unsubmitted` | UC-O8 | ★ **两栏**：`never_submitted[]` · `changed_since_submit[]`（按 `content_digest` 判） |

### 1.2 ③ 采购域

| 方法 | 路径 | 用例 | 关键点 |
|---|---|---|---|
| GET | `/v1/purchase-orders` | UC-P6 | 带领星侧 `status` / `status_shipped` |
| POST | `/v1/purchase-orders` | UC-P3 | ★ 校验领星必填：单价 · 采购员 · 采购方 · 仓库 · 供应商；**一单一供应商** |
| GET | `/v1/purchase-orders/{po_id}` | UC-P6 | 带承重墙①来源明细 |
| PUT | `/v1/purchase-orders/{po_id}` | UC-P4 | ★ 已下单 → 409 `po_frozen` |
| DELETE | `/v1/purchase-orders/{po_id}` | UC-P4 | 同上 |
| **POST** | **`/v1/purchase-orders/{po_id}/place`** | UC-P5 | ⛔ **不可逆**。见 §1.5 |
| GET | `/v1/purchase-orders/{po_id}/progress` | UC-P6 | 到货明细 + ★ **到货落在哪个仓**（C4） |

### 1.3 ⑤ 排货域

| 方法 | 路径 | 用例 | 关键点 |
|---|---|---|---|
| GET | `/v1/dispatch-candidates` | UC-D1 | ★ 按**待排量 > 0** 筛，不按状态值筛 |
| GET | `/v1/stock/local` | UC-D2 | 入口 B；★ 每行带 `plan_lines[]`，**查不到就标红** |
| POST | `/v1/sheets` | UC-D3 | 建空草稿，记建表水位 |
| GET | `/v1/sheets` · `/v1/sheets/{id}` | UC-D3 | 表 + 排货单 + 行 + 需求构成 |
| POST | `/v1/sheets/{id}/lines` | UC-D4 | ★ **每次重建整表**，返回 `restored[]` / `dropped[]` |
| POST | `/v1/sheets/{id}/consignments` | UC-D6 | 建排货单（目的地 + channel） |
| **PUT** | **`/v1/sheets/{id}/consignments/{cid}`** | UC-D6 | ★ **G4 补**：改目的地 / channel。★ 仅草稿；已确认 → 409 `sheet_frozen` |
| **DELETE** | **`/v1/sheets/{id}/consignments/{cid}`** | UC-D6 | ★ **G4 补**：删空排货单。★ 有行则 409 `consignment_not_empty` |
| PUT | ★ `/v1/sheets/{id}/consignments/{cid}/lines/{sku}/{seller_id}/{from_wid}` | UC-D5 / UC-D7 | 源仓 · ★ **店铺**（必填）· 数量 · **装箱**（A-8 人工填）。★ **不收货件号、不收 FC** —— 投送后才有，且归属是 N:M（承重墙③）。见 `04` A-6 那一节 |
| **DELETE** | **`/v1/sheets/{id}/consignments/{cid}/lines/{sku}/{from_wid}`** | UC-D4 | ★ **G5 补**：移除一行。★ 同时删承重墙②映射，触发再评估 |
| **POST** | **`/v1/sheets/{id}/confirm`** | UC-D8 | 🔒 **6 项闸**，见 §3 错误码 |
| POST | `/v1/sheets/{id}/cancel` | UC-D12 | 人工放弃 |
| GET | ★ `/v1/consignments/{cid}/placement-options` | ★ **O-7** | 亚马逊给的选项：各自的货件拆法 · FC · **费用**。★ 只读，不自动选 |
| **POST** | ★ `/v1/consignments/{cid}/placement-options/{option_id}/choose` | ★ **O-7** | 🔒 **人的决策点**，留痕：选了哪项 · 当时费用 · 谁选的 |
| **POST** | **`/v1/consignments/{cid}/dispatch`** | UC-D9 | ⛔ **不可逆**。见 §1.5 |
| GET | ★ `/v1/consignments/{cid}/splits` | ★ **O-2** | 回读来的货件与其货号明细（N:M 带数量）；★ 必须能表达「**归属未知**」，不许当 0 |
| POST | `/v1/consignments/{cid}/abandon` | UC-D12 | ★ 仅草稿；已投送 → 422 |
| GET | `/v1/ledger` | UC-D10 | 已排未发 / 已投送未完成 + 停滞告警 |
| GET | `/v1/unlinked-documents` | UC-D11 | ★ 命名避开 `shipment` |
| POST | `/v1/unlinked-documents/ack` | UC-D11 | 只追加，不可撤回 |

### 1.4 预测 · 维度 · 系统

| 方法 | 路径 | 关键点 |
|---|---|---|
| POST | `/v1/forecast` | UC-F2。★ **G7 订正**：入参见 §1.6；**不落库**；★ 上限 **500** 个 `(sku, seller_id)`（H-2） |
| GET | `/v1/skus` · `/v1/sellers` · `/v1/warehouses` · `/v1/categories` | 维度；★ 品类带 `refreshed_at` 与**陈旧标注** |
| GET | `/health` | ★ 不加前缀 |
| GET | `/v1/readiness` | 镜像新鲜度 · 迁移版本 · 领星连通性 |

### 1.5 ★ 两个不可逆接口的额外约定

```
POST /v1/purchase-orders/{po_id}/place
POST /v1/consignments/{cid}/dispatch
```

| 约定 | 为什么 |
|---|---|
| ★ 请求体必须带 `confirm_token`（由前一次 GET 返回） | 防误点、防重放 |
| ★ FBA 投送**每张排货单一次**二次确认（B-3） | **不可逆点是逐张跨的** |
| ★ 响应体**必须回显产生的全部外部单号** | 断在哪一步，前端看得出来 |
| ★ 部分成功要**明确表达**，不许当作失败 | 建单成功、发货失败 ≠ 什么都没发生 |

```jsonc
// 部分成功的响应形状
{
  "outcome": "partial",
  "steps": [
    {"stage": "create", "status": "ok",     "doc_no": "OWS2409170001"},
    {"stage": "lock",   "status": "ok"},
    {"stage": "ship",   "status": "manual", "reason": "timeout_needs_verify"}
  ],
  "our_stage": "已确认"     // ★ 未推进到已投送
}
```

★ **`outcome: partial` 这个形状是必须的**：
返回 200 会让前端以为全成了，返回 500 会让人以为什么都没发生 ——
**而真相是「建了单但没发货」，那张单真实存在于领星里。**

### 1.6 ★ G7 订正：`/v1/forecast` 的入参形状

你的原话是「**支持按日期查询，逐周的库存消耗情况，给出断货日**」——
所以入参是**查询起点 + 视野**，不是「月份区间」。

```jsonc
// POST /v1/forecast
{
  "as_of": "2026-09-16",          // ★ 从哪天起算（按日期查询）
  "horizon_weeks": 26,            // 逐周展开多少周
  "targets": [                    // ★ 上限 500（H-2）
    {"sku": "DCC1800264", "seller_id": "11072"}
  ],
  "plan_id": 42                   // 可选：把该计划的计划销量算进需求
}
```

```jsonc
// 响应
{
  "results": [{
    "sku": "...", "seller_id": "...",
    "weeks": [{"week_start": "2026-09-16", "opening": 820, "demand": 140, "supply": 0, "closing": 680}],
    "stockout_date": "2026-11-04",
    "days_of_cover": 49,
    "demand_source": {"history_weeks": 12, "plan_months": ["2026-10","2026-11"]}
  }]
}
```

★ **`demand_source` 必须回显** —— 「这个断货日是拿什么算出来的」，
不回显就没人能判断它对不对。

---

### 1.7 ★ G6 补：运维接口（`/v1/admin/*`）

> ★ **这是我自己在 `00` D-2 里裁定了「另留一个仅运维可用的手动触发」，
> 写接口清单时漏掉的。**

| 方法 | 路径 | 用例 | 关键点 |
|---|---|---|---|
| POST | `/v1/admin/reconcile` | UC-S1 | 手动触发回执；★ 仍**先跑采集面校验**，掉档照样拒 |
| POST | `/v1/admin/mirrors/refresh` | — | 维度镜像刷新；失败 → 服务转 503（E-4） |
| POST | `/v1/admin/categories/refresh` | UC-P2 | 品类镜像刷新；★ `n_dropped` 超阈值**硬失败并点名** |
| GET | `/v1/admin/capture-windows` | UC-S4 | 采集面历史，查「哪天掉档了」 |
| GET | `/v1/admin/erp-calls` | — | 出站调用审计；★ 查「那次超时到底建没建成」 |

★ **这五个都不是 `UC-S2 再评估`** —— 再评估**永远不暴露 HTTP**。
暴露它就等于给了一个**绕过事实直接改状态**的入口。

---

## 2. 我们要对接的外部接口

### 2.1 ★ 领星 —— 写（必经网关）

| 动作 | Path | 幂等键 | 回查 | 副作用 |
|---|---|---|---|---|
| 建采购单 | `/erp/sc/routing/purchase/purchase/createPurchaseOrder` | ★ `custom_order_sn`（我们生成） | `PurchaseOrderList` | ⛔ 建出真单 |
| 建备货单 | `/erp/sc/routing/owms/inbound/createInbound` | ★ `inbound_order_no`（必填唯一） | `listOrderNos` | 不动库存 |
| 备货单锁库存 | `/basicOpen/overSeaWarehouse/stockOrder/allocate` | — | `listInbound` 看 status | **锁定**；★ **无释放接口** |
| **备货单发货** | `/erp/sc/routing/owms/inbound/sendInbound` | — | 同上 | ⛔ **扣减** |
| 建 FBA 发货计划 | `/erp/sc/routing/storage/shipment/createShipmentPlan` | ★ **无** → 自建 `idem_key` | `ShipmentPlanLists` | 不动库存 |
| 发货计划锁库存 | `FbaPlanStorageAllocate` | — | — | **锁定**（有 Release） |
| **STA 建货件** | `/amzStaServer/openapi/inbound-plan/createInboundPlan` 等四步 | ★ **无** | `Operate` 凭 `taskId` 轮询 | ⛔ **真打亚马逊** |
| 建 SP 发货单 | `/erp/sc/routing/storage/shipment/createReadySendOrder` | ★ **无** | — | 不动库存；🔒 `shipment_id` + `box_list` 必填 |
| **FBA 发货** | `/erp/sc/storage/shipment/sendGoods` | — | — | ⛔ **扣减** |

★ **按 path 前缀判副作用等级**：`/amzStaServer/` = 打到亚马逊、不可逆；`/erp/` = 只改领星本地。
**按接口名判会错** —— `CreateShipmentPlan` 与 `CreateSTATask` 名字太像。

★ **STA 一族没有客户侧幂等键**，只能事后轮询 —— 所以它的失败处置只有一条：**转人工**。

### 2.2 领星 —— 读（可选，主要走 CH）

| 用途 | Path | 什么时候用 |
|---|---|---|
| 超时查重 | `listOrderNos` / `PurchaseOrderList` / `ShipmentPlanLists` / `searchProcessResult` | ★ **只在 `NEEDS_VERIFY` 时调** |
| 轮询 STA | `task-plan/operate` | 建货件后拿单号 |
| 供应商列表 | `/erp/sc/data/local_inventory/supplier` | 镜像刷新 |

★ **常规读一律走 CH，不走领星** —— 领星实时但慢，不适合高频。

### 2.3 CH `jxd_raw` —— 只读

| 用途 | 表 | ★ 继承的实测教训 |
|---|---|---|
| 货号全集 | 商品目录 **LEFT JOIN** 快照 | 直接查快照 → **断货 SKU 整个消失** |
| msku ↔ 货号 | 桥表 | ★ 按 as_of `argMax`，但 **sid 绝不参与** |
| FBA 库存 | `lingxing_inventory_fba_detail` | ★ **不要用** `amazon_sp_api_report_fba_inventory`（把 UK/DE/PL/FR/IT 混成一行） |
| 本地仓可发 | `lingxing_inventory_warehouse` | ★ **只读 `product_valid_num`**，禁止总量相减 |
| 单据快照 | `lingxing_*` | ★ 取 `max(_captured_date)`，**不做认知时间回放** |

### 2.4 PM 产品系统

| 用途 | 方式 |
|---|---|
| 品类树 | Keycloak service account 换 token → Bearer 打 `/api/integration/*`；★ secret **不落盘** |

---

## 3. 错误码总表

| code | 码 | 含义 |
|---|---|---|
| `msku_already_claimed` | 409 | 被某个**尚未下单**的计划占用；detail 带占用方 |
| `another_rev_in_flight` | 409 | 该计划已有一版在流转 |
| `reason_required` | 400 | 撤销 / 取消 / 放弃**必须填理由** |
| `po_frozen` | 409 | 采购单已下单，不可改 |
| `sheet_frozen` | 409 | 排货表已确认，不可改 |
| `no_plan_line_for_demand` | 409 | ★ **B4**：没有计划单记录，**先补销售计划** |
| `packing_incomplete` | 409 | G1 装箱数据不齐 |
| ★ `dest_incomplete` | 409 | G2 FBA 排货单缺店铺 `sid`（★ 不含 FC）；★ 原 `shipment_unassigned` 作废 |
| `insufficient_available` | 409 | G3 可发量不足（按 `product_valid_num`） |
| `ledger_moved` | 409 | G4 撞别的排货表的已排未发，**要求重建** |
| `unlinked_documents_pending` | 409 | G5 未关联发货未清零 |
| `invalid_destination` | 409 | G6 目的地不合法 |
| `illegal_transition` | 422 | 非法状态迁移；★ detail **回显 `allowed[]`** |
| `terminal_state` | 422 | 从终态出发 |
| `mirror_stale` | 503 | ★ 维度镜像陈旧，**拒绝服务** |
| `unknown_query_param` | 400 | ★ 未声明的查询参数 |

★ **`illegal_transition` 必须回显 `allowed[]`** —— 只说「不许」而不说「那能去哪」，前端只能猜。

---

## 4. `service.toml` 要点

```toml
[service]
base_url    = "http://<内网 IP>:<port>"
path_prefix = "/v1"              # ★ 转发目标 = base_url + path_prefix + serves.path

[health]
path = "/health"                 # ★ 探活目标 = base_url + health.path，不加前缀
```

★ 改契约要同步跑契约测试；★ `service.toml` **必须在仓库根目录**。

---

## 5. 悬而未决

> ★ 未决项只在 `00-待裁定清单.md` 一处维护，本文只引用。

| # | 问题 | 状态 |
|---|---|---|
| H-1 | `confirm_token` 怎么生成 | ★ **已定**：`sha256(类型 + id + 内容摘要 + 盐)`，**不存储、无过期** —— 内容一变自然失效 |
| H-2 | `/v1/forecast` 入参上限 | ★ **已定**：**500** 个 `(sku, seller)`，超出 400 `too_many_targets` |
| C-1~C-5 | 领星实调项 | ★ P0，仅存的未决 |
